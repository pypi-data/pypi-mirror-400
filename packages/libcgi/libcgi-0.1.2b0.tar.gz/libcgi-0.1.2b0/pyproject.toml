[build-system]
requires        = ["setuptools"]
build-backend   = "setuptools.build_meta"

[project]
name            = "libcgi"
version         = "0.1.2b"
description     = "軽量CGIモジュール"
authors         = [
    { name      = "PEN" }
]
readme          = "README.md"
requires-python = ">=3.12"

[tool.setuptools]
packages = [
    "pycgi",
    "pycgitb"
]
