[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cyberlicensing"
version = "0.3.0"
description = "Client and manager helpers for CyberLicensing"
authors = [
    { name = "CyberLicensing", email = "me@showdown.boo" },
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
]

[project.urls]
Homepage = "https://licensing.showdown.boo/"
