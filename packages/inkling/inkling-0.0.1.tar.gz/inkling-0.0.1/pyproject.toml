[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "inkling"
version = "0.0.1"
description = "Coming soon."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    { name = "Inkling Team" }
]
classifiers = [
    "Development Status :: 1 - Planning",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]

[project.urls]
Homepage = "https://github.com/katiyar/inkling"
