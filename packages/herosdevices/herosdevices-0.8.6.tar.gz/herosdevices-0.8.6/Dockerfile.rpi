# #########################################################################################
# Build image with everything required to run HEROS devices via BOSS/HEROS
# #########################################################################################

# -------- Final Stage --------
FROM registry.gitlab.com/atomiq-project/herosdevices

# Install dependencies for Raspberry Pi
RUN  pip install --break-system-packages -e herosdevices[rpi]
