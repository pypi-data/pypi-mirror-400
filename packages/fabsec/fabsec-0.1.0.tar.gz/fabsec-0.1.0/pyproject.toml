[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fabsec"
version = "0.1.0"
description = "FabSec SDK – Security & AI Platform"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]


authors = [
  {name = "Sudofab", email = "sudomata@gmail.com"}
]

[tool.setuptools]
packages = ["fabsec"]
