[tool.poetry]
name = "dat1-runtime"
version = "0.1.4"
license = "GPL-3.0-only"
description = "Dat1.co runtime helper library"
authors = [
    "Dat1.co Team <contact@dat1.co>"
]
packages = [{ include = "dat1" }]

[tool.poetry.dependencies]


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
