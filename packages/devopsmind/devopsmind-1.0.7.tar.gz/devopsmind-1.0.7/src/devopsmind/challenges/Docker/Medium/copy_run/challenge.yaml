id: copy_run
title: "Dockerfile that copies a local script and runs it"
stack: docker
difficulty: Medium
xp: 100
goal: "Build a Docker image that copies application code into the image and runs it at container startup"
skills:
  - dockerfile-authoring
  - image-build-process
  - container-execution
hint: "Dockerfile must COPY app.py and set CMD to run python3 app.py."
validator: "validator.py"
