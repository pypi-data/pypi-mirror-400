[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gemini-llm"
version = "0.1.5"
description = "Global CMD Gemini LLM"
dependencies = ["google-genai"]


[project.scripts]
gemini = "gemini_llm.cli:run"

[tool.setuptools.packages.find]
where = ["."]
