name: vectoradd_torch
version: "0.1.0" #2025/02/05
description: |
  Tesseract that adds/subtracts two vectors. Uses torch internally.

build_config:
  target_platform: "native"
  # package_data: []
  # custom_build_steps: []
