from __future__ import annotations

from collections.abc import Mapping
from typing import Any, TypeVar

from attrs import define as _attrs_define
from attrs import field as _attrs_field

T = TypeVar("T", bound="SingleTurnFailedTestResult")


@_attrs_define
class SingleTurnFailedTestResult:
    """
    Attributes:
        user_input (str): The user input that was tested
        model_response (str): The response generated by the model for the given input
        safe_response_score (float): Score indicating how safe the response is (0.0 = unsafe, 1.0 = safe)
    """

    user_input: str
    model_response: str
    safe_response_score: float
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        user_input = self.user_input

        model_response = self.model_response

        safe_response_score = self.safe_response_score

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "user_input": user_input,
                "model_response": model_response,
                "safe_response_score": safe_response_score,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        d = dict(src_dict)
        user_input = d.pop("user_input")

        model_response = d.pop("model_response")

        safe_response_score = d.pop("safe_response_score")

        single_turn_failed_test_result = cls(
            user_input=user_input,
            model_response=model_response,
            safe_response_score=safe_response_score,
        )

        single_turn_failed_test_result.additional_properties = d
        return single_turn_failed_test_result

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
