[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"


[tool.pytest.ini_options]
markers = [
    "smoke: small set of tests used for basic functionality verification",
]