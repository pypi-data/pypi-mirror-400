[project]
name = "hiagent_current_time_mcp"
version = "0.1.3"
description = "The MCP server to get local time"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.14.2",
]

[tool.uv.workspace]
members = ["venv"]

[project.scripts]
hiagent_current_time_mcp = "hiagent_current_time_mcp.server:main"
