[project]
name = "mcp-document-processor"
version = "0.1.4"
description = "A simple document processor MCP server"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "dever1995" }]
dependencies = ["mcp>=1.25.0"]

[project.scripts]
mcp-document-processor = "mcp_document_processor:main"