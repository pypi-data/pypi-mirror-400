[project]
name = "bombsquad"
version = "0.1.0"
description = "Isolate OOM-prone function in process, raise exception on SIGKILL and Segfault"
authors = [
    {name = "Ben Skubi",email = "skubi@ohsu.edu"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dill"
]

[dependency-groups]
dev = [
    "pytest"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
