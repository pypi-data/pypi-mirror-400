### ⚙️ Challenge — Debug DNS Lookup (Offline)

You are provided a DNS failure output in `dns_failure.log`.

Your task:
1. Write a script `diagnose_dns.sh`
2. The script must analyze `dns_failure.log`
3. Print the root cause of the failure

Expected output:
No DNS servers reachable
