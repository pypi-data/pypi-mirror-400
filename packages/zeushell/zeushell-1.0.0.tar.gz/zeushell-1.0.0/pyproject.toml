[project]
name = "zeushell"
version = "1.0.0"
description ="A small Python package published to PyPI"
authors = [
    {name = "Zeus", email = "zeus@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
keywords = ["example", "package", "pypi"]
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
