[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "surf_scrap"
version = "0.0.2"
authors = [
  { name = "Rene" },
  { name = "Cybelle" }
]
description = "Une bibliothèque pour scraper les données de surf-report.com"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests",
    "beautifulsoup4",
    "pandas",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["surf_scrap*"]