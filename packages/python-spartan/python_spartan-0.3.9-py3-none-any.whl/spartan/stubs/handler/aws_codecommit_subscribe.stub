
def main(event, context):
    for record in event['Records']:
        event_name = record['eventName']
        repo_name = record['codecommit']['references'][0]['repositoryName']
        print(f"Event {event_name} occurred in repository: {repo_name}")

    # Implement your code here...

    return {
        "StatusCode": 200
    }
