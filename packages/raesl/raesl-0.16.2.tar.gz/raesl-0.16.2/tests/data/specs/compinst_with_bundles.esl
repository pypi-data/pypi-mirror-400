define type
    bun is a bundle of
        * a is a real
        * b is an integer

define component Cbun
    parameters
        p is a bun

define component Creal
    parameters
        r is a real

world
    variables
        u is a real
        v is an integer
        w is a bun

    variable-group
        uv is a group of
            * u
            * w.b

    components
        cb1 is a Cbun with arguments
            * w

        cb2 is a Cbun with arguments
            * uv

        cr1 is a Creal with arguments
            * u

        cr2 is a Creal with arguments
            * w.a
