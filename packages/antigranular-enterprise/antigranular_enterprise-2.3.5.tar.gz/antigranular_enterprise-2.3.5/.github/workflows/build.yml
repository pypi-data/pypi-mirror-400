name: Call SonarCloud Scan

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master

jobs:
  sonar-scan:
    uses: ObliviousAI/workflow_templates/.github/workflows/sonar_scan.yml@dev
    secrets: inherit