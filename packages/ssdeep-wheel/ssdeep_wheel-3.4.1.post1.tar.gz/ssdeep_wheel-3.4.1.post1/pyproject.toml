[build-system]
# Must be kept in sync with `setup_requirements` in `setup.py`
requires = [
    "setuptools>=40.8.0",
    "wheel",
    "cffi>=1.12; python_implementation != 'PyPy'",  # for abi3, see https://cffi.readthedocs.io/en/stable/cdef.html
]
build-backend = "setuptools.build_meta"
