[build-system]
requires = ["pybind11==3.0.1", "setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
test-command = "python {project}/tests/test.py"

[tool.ruff]
target-version = "py39"