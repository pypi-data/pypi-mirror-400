syntax = "proto3";

package strique.model.widget.tiktok.leadgen;

import "constants/ui/render_type.proto";
import "constants/ui/sorting_direction.proto";
import "options/strique_enum_options.proto";
import "types/currency/currency.proto";
import "types/filter_predicate/filters.proto";

option java_outer_classname = "TikTokLeadGenAdsTrendAnalysisProto";
option java_package = "com.strique.model.widget.tiktok.leadgen";

// Next Id: 6
// Proto definition for TikTok Ads Trend Analysis Data.
message TikTokLeadGenAdsTrendAnalysisData {
  // Title for this widget.
  string widget_title = 1;

  // List of records for this widget.
  repeated TikTokLeadGenAdsTrendAnalysisRecord records = 2;

  // Total record.
  optional TikTokLeadGenAdsTrendAnalysisRecord total = 3;

  // Summary data for a subset of records.
  optional TikTokLeadGenAdsTrendAnalysisRecord subtotal = 4;

  //overview message if widget has different currencies.
  optional string overview_message = 5;
}

// Next Id: 73
// Proto definition for a single record of TikTok Ads Trend Analysis.
message TikTokLeadGenAdsTrendAnalysisRecord {
  // date for this record
  string formatted_date = 72 [json_name = "formattedDate"];

  // Cost.
  // Amount spent on advertising.
  types.Currency cost = 2 [json_name = "cost"];

  // CPC (Destination).
  // Average cost for each click (destionation) on the ad.
  types.Currency cost_per_click_destination = 3 [json_name = "costPerClickDestination"];

  // CPM.
  // Cost of 1000 impressions.
  types.Currency cost_per_mille = 4 [json_name = "costPerMille"];

  // Impression.
  int64 impression = 5 [json_name = "impression"];

  // Clicks (destination).
  // Number of clicks (destination).
  int64 clicks_destination = 6 [json_name = "clicksDestination"];

  // CTR (destination).
  // Percentage of impressions resulting in a click.
  float click_through_rate_destination = 7 [json_name = "clickThroughRateDestination"];

  // Reach.
  int64 reach = 8 [json_name = "reach"];

  // Frequency.
  // Total impressions per reach.
  float frequency = 9 [json_name = "frequency"];

  // Conversion.
  // Number of conversions.
  int64 conversion = 10 [json_name = "conversion"];

  // Cost per conversion.
  // Amount spent per conversion.
  types.Currency cost_per_conversion = 11 [json_name = "costPerConversion"];

  // Conversion rate (CVR).
  // Percentage of clicks on the ad that resulted in a conversion.
  float conversion_rate = 12 [json_name = "conversionRate"];

  // Clicks (all).
  // Total number of clicks (all).
  int64 clicks_all = 13 [json_name = "clicksAll"];

  // CTR (all).
  // Percentage of impressions resulting in a click.
  float click_through_rate_all = 14 [json_name = "clickThroughRateAll"];

  // Subscribe (page).
  // Number of subscriptions (page).
  int64 subscribe_page = 15 [json_name = "subscribePage"];

  // Cost per subscribe (page).
  // Amount spent per subscription (page).
  types.Currency cost_per_subscribe_page = 16 [json_name = "costPerSubscribePage"];

  // Value per subscribe (page).
  // Value per subscription (page).
  types.Currency value_per_subscribe_page = 17 [json_name = "valuePerSubscribePage"];

  // Subscribe value (page).
  // Total value from subscriptions (page).
  types.Currency subscribe_value_page = 18 [json_name = "subscribeValuePage"];

  // Form submission (page).
  // Number of form submissions (page).
  int64 form_submission_page = 19 [json_name = "formSubmissionPage"];

  // Cost per submitted (page).
  // Amount spent per form submission (page).
  types.Currency cost_per_submitted_page = 20 [json_name = "costPerSubmittedPage"];

  // Value per form submit (page).
  // Value per form submission (page).
  types.Currency value_per_form_submit_page = 21 [json_name = "valuePerFormSubmitPage"];

  // Form submission value (page).
  // Total value from form submissions (page).
  types.Currency form_submission_value_page = 22 [json_name = "formSubmissionValuePage"];

  int64 result = 23 [json_name = "result"];

  types.Currency cost_per_result = 24 [json_name = "costPerResult"];

  // Video views
  // total number of video views.
  int64 video_views = 25 [json_name = "videoViews"];

  // 2-second Video views
  // number of 2-second video views.
  int64 video_views_2_seconds = 26 [json_name = "videoViews2Seconds"];

  // 6-second Video views
  // number of 6-second video views.
  int64 video_views_6_seconds = 27 [json_name = "videoViews6Seconds"];

  // 6-second focused views
  // number of 6-second focused video views.
  int64 video_views_6_seconds_focused = 28 [json_name = "videoViews6SecondsFocused"];

  // 15-second focused views
  // number of 15-second focused video views.
  int64 video_views_15_seconds_focused = 29 [json_name = "videoViews15SecondsFocused"];

  // Video views at 100%
  // number of video views watched to completion (100%).
  int64 video_views_100_percent = 30 [json_name = "videoViews100Percent"];

  // Video views at 75%
  // number of video views watched up to 75%.
  int64 video_views_75_percent = 31 [json_name = "videoViews75Percent"];

  // Video views at 50%
  // number of video views watched up to 50%.
  int64 video_views_50_percent = 32 [json_name = "videoViews50Percent"];

  // Video views at 25%
  // number of video views watched up to 25%.
  int64 video_views_25_percent = 33 [json_name = "videoViews25Percent"];

  // Average play time per video view
  // average duration of play time per video view.
  float average_play_time_per_video_view = 34 [json_name = "averagePlayTimePerVideoView"];

  // Average play time per user
  // average duration of play time per user.
  float average_play_time_per_user = 35 [json_name = "averagePlayTimePerUser"];

  // Total landing page views
  // total number of landing page views.
  int64 total_landing_page_views = 36 [json_name = "totalLandingPageViews"];

  // Cost per landing page view
  // amount spent per landing page view.
  types.Currency cost_per_landing_page_view = 37 [json_name = "costPerLandingPageView"];

  // Landing page view rate
  // percentage of visitors viewing the landing page.
  float landing_page_view_rate = 38 [json_name = "landingPageViewRate"];

  // Total page view
  // total number of page views.
  int64 total_page_view = 39 [json_name = "totalPageView"];

  // Cost per page view
  // amount spent per page view.
  types.Currency cost_per_page_view = 40 [json_name = "costPerPageView"];

  // Page view rate %
  // percentage of visitors viewing the page.
  float page_view_rate = 41 [json_name = "pageViewRate"];

  int64 button_click_page = 42 [json_name = "buttonClickPage"];

  types.Currency cost_per_button_click_page = 43 [json_name = "costPerButtonClickPage"];

  float unique_button_click_rate_page = 44 [json_name = "uniqueButtonClickRatePage"];

  types.Currency value_per_button_click_page = 45 [json_name = "valuePerButtonClickPage"];

  types.Currency total_button_click_value_page = 46 [json_name = "totalButtonClickValuePage"];

  int64 contact_page = 47 [json_name = "contactPage"];

  types.Currency cost_per_contact_page = 48 [json_name = "costPerContactPage"];

  float unique_contact_rate_page = 49 [json_name = "uniqueContactRatePage"];

  types.Currency value_per_contact_page = 50 [json_name = "valuePerContactPage"];

  types.Currency contact_value_page = 51 [json_name = "contactValuePage"];

  int64 registration_complete_page = 52 [json_name = "registrationCompletePage"];

  types.Currency cost_per_registration_complete_page = 53 [json_name = "costPerRegistrationCompletePage"];

  float unique_registration_complete_rate_page = 54 [json_name = "uniqueRegistrationCompleteRatePage"];

  types.Currency value_per_registration_complete_page = 55 [json_name = "valuePerRegistrationCompletePage"];

  types.Currency registration_complete_value_page = 56 [json_name = "registrationCompleteValuePage"];

  int64 content_view_page = 57 [json_name = "contentViewPage"];

  types.Currency cost_per_content_view_page = 58 [json_name = "costPerContentViewPage"];

  float unique_content_view_rate_page = 59 [json_name = "uniqueContentViewRatePage"];

  types.Currency value_per_content_view_page = 60 [json_name = "valuePerContentViewPage"];

  types.Currency content_view_value_page = 61 [json_name = "contentViewValuePage"];

  int64 downloads_page = 62 [json_name = "downloadsPage"];

  types.Currency cost_per_downloads_page = 63 [json_name = "costPerDownloadsPage"];

  float unique_downloads_rate_page = 64 [json_name = "uniqueDownloadsRatePage"];

  types.Currency value_per_downloads_page = 65 [json_name = "valuePerDownloadsPage"];

  types.Currency downloads_value_page = 66 [json_name = "downloadsValuePage"];

  int64 custom_event_web = 67 [json_name = "customEventWeb"];

  types.Currency cost_per_custom_event_web = 68 [json_name = "costPerCustomEventWeb"];

  float unique_custom_event_rate_web = 69 [json_name = "uniqueCustomEventRateWeb"];

  types.Currency value_per_custom_event_web = 70 [json_name = "valuePerCustomEventWeb"];

  types.Currency custom_event_value_web = 71 [json_name = "customEventValueWeb"];

  reserved 1;
}

// Next Id: 4
// Proto definition for TikTok Ads Trend Analysis Configuration.
message TikTokLeadGenAdsTrendAnalysisConfig {
  // Next Id: 72
  // Enumeration of possible columns in the TikTok Ads Trend Analysis table.
  enum Column {
    COLUMN_UNKNOWN = 0 [
      (options.label) = 'Unknown',
      (options.accessor_key) = '',
      (options.is_sortable) = false,
      (options.is_filterable) = false,
      (options.is_dropdown_not_support) = true
    ];
    COLUMN_DATE = 1 [
      (options.label) = 'Date',
      (options.accessor_key) = 'formattedDate',
      (options.is_sortable) = true,
      (options.is_filterable) = false,
      (options.is_dropdown_not_support) = true
    ];
    COLUMN_COST = 2 [
      (options.label) = 'Cost',
      (options.accessor_key) = 'cost',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CLICK_DESTINATION = 3 [
      (options.label) = 'Cost per click (destination)',
      (options.accessor_key) = 'costPerClickDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_MILLE = 4 [
      (options.label) = 'CPM',
      (options.accessor_key) = 'costPerMille',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_IMPRESSION = 5 [
      (options.label) = 'Impression',
      (options.accessor_key) = 'impression',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICKS_DESTINATION = 6 [
      (options.label) = 'Clicks (destination)',
      (options.accessor_key) = 'clicksDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICK_THROUGH_RATE_DESTINATION = 7 [
      (options.label) = 'Click-through rate (destination)',
      (options.accessor_key) = 'clickThroughRateDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_REACH = 8 [
      (options.label) = 'Reach',
      (options.accessor_key) = 'reach',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FREQUENCY = 9 [
      (options.label) = 'Frequency',
      (options.accessor_key) = 'frequency',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONVERSION = 10 [
      (options.label) = 'Conversion',
      (options.accessor_key) = 'conversion',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CONVERSION = 11 [
      (options.label) = 'Cost per conversion',
      (options.accessor_key) = 'costPerConversion',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONVERSION_RATE = 12 [
      (options.label) = 'Conversion rate (CVR)',
      (options.accessor_key) = 'conversionRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_CLICKS_ALL = 13 [
      (options.label) = 'Clicks (all)',
      (options.accessor_key) = 'clicksAll',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICK_THROUGH_RATE_ALL = 14 [
      (options.label) = 'Click-through rate (all)',
      (options.accessor_key) = 'clickThroughRateAll',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_SUBSCRIBE_PAGE = 15 [
      (options.label) = 'Subscribe (page)',
      (options.accessor_key) = 'subscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_SUBSCRIBE_PAGE = 16 [
      (options.label) = 'Cost per subscribe (page)',
      (options.accessor_key) = 'costPerSubscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VALUE_PER_SUBSCRIBE_PAGE = 17 [
      (options.label) = 'Value per subscribe (page)',
      (options.accessor_key) = 'valuePerSubscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_SUBSCRIBE_VALUE_PAGE = 18 [
      (options.label) = 'Subscribe value (page)',
      (options.accessor_key) = 'subscribeValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FORM_SUBMISSION_PAGE = 19 [
      (options.label) = 'Form submission (page)',
      (options.accessor_key) = 'formSubmissionPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_SUBMITTED_PAGE = 20 [
      (options.label) = 'Cost per submitted (page)',
      (options.accessor_key) = 'costPerSubmittedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VALUE_PER_FORM_SUBMIT_PAGE = 21 [
      (options.label) = 'Value per form submit (page)',
      (options.accessor_key) = 'valuePerFormSubmitPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FORM_SUBMISSION_VALUE_PAGE = 22 [
      (options.label) = 'Form submission value (page)',
      (options.accessor_key) = 'formSubmissionValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_RESULT = 23 [
      (options.label) = 'Result',
      (options.accessor_key) = 'result',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_RESULT = 24 [
      (options.label) = 'Cost per result',
      (options.accessor_key) = 'costPerResult',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW = 25 [
      (options.label) = 'Video view',
      (options.accessor_key) = 'videoViews',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_2_SECONDS = 26 [
      (options.label) = 'Video view (2 seconds)',
      (options.accessor_key) = 'videoViews2Seconds',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_6_SECONDS = 27 [
      (options.label) = 'Video view (6 seconds)',
      (options.accessor_key) = 'videoViews6Seconds',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_6_SECONDS_FOCUSED = 28 [
      (options.label) = 'Video view (6 seconds focused)',
      (options.accessor_key) = 'videoViews6SecondsFocused',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_15_SECONDS_FOCUSED = 29 [
      (options.label) = 'Video view (15 seconds focused)',
      (options.accessor_key) = 'videoViews15SecondsFocused',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_25_PERCENT = 30 [
      (options.label) = 'Video view (25%)',
      (options.accessor_key) = 'videoViews25Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_50_PERCENT = 31 [
      (options.label) = 'Video view (50%)',
      (options.accessor_key) = 'videoViews50Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_75_PERCENT = 32 [
      (options.label) = 'Video view (75%)',
      (options.accessor_key) = 'videoViews75Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEW_100_PERCENT = 33 [
      (options.label) = 'Video view (100%)',
      (options.accessor_key) = 'videoViews100Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_AVERAGE_PLAY_TIME_PER_VIDEO_VIEW = 34 [
      (options.label) = 'Avg. play time per video view',
      (options.accessor_key) = 'averagePlayTimePerVideoView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_AVERAGE_PLAY_TIME_PER_USER = 35 [
      (options.label) = 'Avg. play time per user',
      (options.accessor_key) = 'averagePlayTimePerUser',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_LANDING_PAGE_VIEWS = 36 [
      (options.label) = 'Total landing page views',
      (options.accessor_key) = 'totalLandingPageViews',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_LANDING_PAGE_VIEWS = 37 [
      (options.label) = 'Cost per landing page views',
      (options.accessor_key) = 'costPerLandingPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_LANDING_PAGE_VIEW_RATE = 38 [
      (options.label) = 'Landing page view rate',
      (options.accessor_key) = 'landingPageViewRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_TOTAL_PAGE_VIEW = 39 [
      (options.label) = 'Total page view',
      (options.accessor_key) = 'totalPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_PAGE_VIEW = 40 [
      (options.label) = 'Cost per page view',
      (options.accessor_key) = 'costPerPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_PAGE_VIEW_RATE = 41 [
      (options.label) = 'Page view rate',
      (options.accessor_key) = 'pageViewRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_BUTTON_CLICK_PAGE = 42 [
      (options.label) = 'Button click (page)',
      (options.accessor_key) = 'buttonClickPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_BUTTON_CLICK_PAGE = 43 [
      (options.label) = 'Cost per button click (page)',
      (options.accessor_key) = 'costPerButtonClickPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_BUTTON_CLICK_RATE_PAGE = 44 [
      (options.label) = 'Unique button click rate (page)',
      (options.accessor_key) = 'uniqueButtonClickRatePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_BUTTON_CLICK_PAGE = 45 [
      (options.label) = 'Value per button click (page)',
      (options.accessor_key) = 'valuePerButtonClickPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_BUTTON_CLICK_VALUE_PAGE = 46 [
      (options.label) = 'Total button click value (page)',
      (options.accessor_key) = 'totalButtonClickValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONTACT_PAGE = 47 [
      (options.label) = 'Contact (page)',
      (options.accessor_key) = 'contactPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CONTACT_PAGE = 48 [
      (options.label) = 'Cost per contact (page)',
      (options.accessor_key) = 'costPerContactPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_CONTACT_RATE_PAGE = 49 [
      (options.label) = 'Unique contact rate (page)',
      (options.accessor_key) = 'uniqueContactRatePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_CONTACT_PAGE = 50 [
      (options.label) = 'Value per contact (page)',
      (options.accessor_key) = 'valuePerContactPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_CONTACT_VALUE_PAGE = 51 [
      (options.label) = 'Total contact value (page)',
      (options.accessor_key) = 'contactValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_REGISTRATION_COMPLETE_PAGE = 52 [
      (options.label) = 'Registration complete (page)',
      (options.accessor_key) = 'registrationCompletePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_REGISTRATION_COMPLETE_PAGE = 53 [
      (options.label) = 'Cost per registration complete (page)',
      (options.accessor_key) = 'costPerRegistrationCompletePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_REGISTRATION_COMPLETE_RATE_PAGE = 54 [
      (options.label) = 'Unique registration complete rate (page)',
      (options.accessor_key) = 'uniqueRegistrationCompleteRatePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_REGISTRATION_COMPLETE_PAGE = 55 [
      (options.label) = 'Value per registration complete (page)',
      (options.accessor_key) = 'valuePerRegistrationCompletePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_REGISTRATION_COMPLETE_VALUE_PAGE = 56 [
      (options.label) = 'Total registration complete value (page)',
      (options.accessor_key) = 'registrationCompleteValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONTENT_VIEW_PAGE = 57 [
      (options.label) = 'Content view (page)',
      (options.accessor_key) = 'contentViewPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CONTENT_VIEW_PAGE = 58 [
      (options.label) = 'Cost per content view (page)',
      (options.accessor_key) = 'costPerContentViewPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_CONTENT_VIEW_RATE_PAGE = 59 [
      (options.label) = 'Unique content view rate (page)',
      (options.accessor_key) = 'uniqueContentViewRatePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_CONTENT_VIEW_PAGE = 60 [
      (options.label) = 'Value per content view (page)',
      (options.accessor_key) = 'valuePerContentViewPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_CONTENT_VIEW_VALUE_PAGE = 61 [
      (options.label) = 'Total content view value (page)',
      (options.accessor_key) = 'contentViewValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_DOWNLOAD_PAGE = 62 [
      (options.label) = 'Download (page)',
      (options.accessor_key) = 'downloadsPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_DOWNLOAD_PAGE = 63 [
      (options.label) = 'Cost per download (page)',
      (options.accessor_key) = 'costPerDownloadsPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_DOWNLOAD_RATE_PAGE = 64 [
      (options.label) = 'Unique download rate (page)',
      (options.accessor_key) = 'uniqueDownloadsRatePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_DOWNLOAD_PAGE = 65 [
      (options.label) = 'Value per download (page)',
      (options.accessor_key) = 'valuePerDownloadsPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_DOWNLOAD_VALUE_PAGE = 66 [
      (options.label) = 'Total download value (page)',
      (options.accessor_key) = 'downloadsValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CUSTOM_EVENT_WEB = 67 [
      (options.label) = 'Custom event (web)',
      (options.accessor_key) = 'customEventWeb',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CUSTOM_EVENT_WEB = 68 [
      (options.label) = 'Cost per custom event (web)',
      (options.accessor_key) = 'costPerCustomEventWeb',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_CUSTOM_EVENT_RATE_WEB = 69 [
      (options.label) = 'Unique custom event rate (web)',
      (options.accessor_key) = 'uniqueCustomEventRateWeb',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_CUSTOM_EVENT_WEB = 70 [
      (options.label) = 'Value per custom event (web)',
      (options.accessor_key) = 'valuePerCustomEventWeb',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_CUSTOM_EVENT_VALUE_WEB = 71 [
      (options.label) = 'Total custom event value (web)',
      (options.accessor_key) = 'customEventValueWeb',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
  }

  // Next Id: 8
  // Configuration for the table displayed in the TikTok Ads TrendAnalysis.
  message TableConfig {
    // Specifies the order of columns in the table.
    repeated Column column_order = 1;

    // Specifies columns to be hidden in the table.
    repeated Column column_hidden = 2;

    // Specifies the default column used for sorting.
    Column default_sorted_column = 3;

    // Specifies the sorting direction.
    constants.ui.SortingDirection sorting_direction = 4;

    // Indicates whether to display the total row in the table.
    bool show_total = 5;

    // Indicates whether to display the subtotal row in the table.
    bool show_subtotal = 6;

    types.Expression filter_expression = 7;
  }

  // Next Id: 3
  // Configuration for the area chart displayed.
  message AreaChartConfig {
    // Specifies the dimension for the area chart.
    Column dimension = 1;

    // Specifies the areas for the area chart.
    repeated Column areas = 2;
  }

  // Specifies the types of rendering for the widget.
  constants.ui.RenderType render_type = 1;

  // Configuration for the table displayed in the widget.
  TableConfig table_config = 2;

  // Configuration for the area chart displayed in the widget.
  AreaChartConfig area_chart_config = 3;
}
