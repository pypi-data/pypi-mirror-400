Analyze InnoDB transactions, locks, and lock waits for troubleshooting concurrency issues.

**Parameters:**
- **info_type** (optional): Type of information to retrieve
  - `'trx'` (default): Active transactions information
  - `'locks'`: Current lock details
  - `'waits'`: Lock wait situations
