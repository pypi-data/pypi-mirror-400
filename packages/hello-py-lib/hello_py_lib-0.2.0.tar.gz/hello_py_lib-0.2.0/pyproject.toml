[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-py-lib"
version = "0.2.0"
description = "A simple hello python library"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
  { name="Your Name", email="your@email.com" }
]

dependencies = []

[project.urls]
Homepage = "https://github.com/yourname/hello_py_lib"
