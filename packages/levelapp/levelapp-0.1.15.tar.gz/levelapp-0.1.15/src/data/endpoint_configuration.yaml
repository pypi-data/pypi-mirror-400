url: "http://127.0.0.1:8000"
api_key: ""
bearer_token: ""
model_id: "meta-llama/Meta-Llama-3.1-8B-Instruct"
payload_path: "../../src/data/payload_example_1.yaml"
payload_template:
  # Add the payload template schema here (no values are required).
  prompt:
  model:
response_template:
  # Add the response template schema here (no values are required).
  reply:
  metadata: