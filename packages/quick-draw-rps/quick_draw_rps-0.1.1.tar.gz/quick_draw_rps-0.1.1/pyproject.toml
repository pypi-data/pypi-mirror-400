[project]
name = "quick-draw-rps"
version = "0.1.1"
description = "Rock Paper Scissors game as a CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
quick-draw = "quick_draw:main"

[build-system]
requires = ["uv_build>=0.8.10,<0.9.0"]
build-backend = "uv_build"
