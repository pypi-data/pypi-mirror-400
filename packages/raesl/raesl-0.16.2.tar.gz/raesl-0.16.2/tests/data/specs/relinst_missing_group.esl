define relation
    myfunc
        requiring parameters
            * p is a real
        returning parameters
            * q is a real

world
    variable
        a is a real

    relations
        mf: myfunc
            requiring arguments
                * a
