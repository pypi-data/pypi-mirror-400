#!/usr/bin/env python
"""
Clouditia SDK - Execute Python and Shell code on remote GPU sessions.

This is a compatibility shim for older pip versions that don't support
pyproject.toml. Modern pip versions will use pyproject.toml directly.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
