define type
  foo is a bundle of
    * x is a real
    * y is a real

  faa is a bundle of
    * q is a foo
    * r is a foo

define verb
  provide to
  transform into

define relation
  model-x
    requiring parameters
      * x is a faa
    returning parameters
      * y is a faa

world
  variables
     s, t is a faa

  components
    c1 is a blob with arguments
      * s
      * t
    c2 is a blob with arguments
      * t
      * s

  goal-requirements
    g1: c1 must provide t to c2
    g2: c2 must provide s to c1

  relations
    r1: model-x
      requiring arguments
       * s
      returning arguments
       * t


define component blob
   parameters
     x, y is a faa

   transformation-requirement
     t1: must transform x into y