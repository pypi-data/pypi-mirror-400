# {generation_note}

from typing import TYPE_CHECKING, TypeAlias

if TYPE_CHECKING:
{imported_symbols}
{tab}{char_union_symbol}: TypeAlias = {char_union_definition}
else:
{placeholder_symbols}
