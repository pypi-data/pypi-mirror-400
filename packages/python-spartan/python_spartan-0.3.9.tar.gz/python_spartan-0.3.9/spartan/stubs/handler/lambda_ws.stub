def websocket_handler(event, context):
    route = event.get('requestContext', {}).get('routeKey')

    if route == "$connect":
        # Handle connection
    elif route == "$disconnect":
        # Handle disconnection
    elif route == "message":
        # Handle custom message route
    else
        # Handle default or unknown route

    return {
        'statusCode': 200,
        'message': 'OK'
    }
