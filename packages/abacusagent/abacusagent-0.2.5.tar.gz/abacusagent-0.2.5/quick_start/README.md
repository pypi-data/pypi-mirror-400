This directory contains a quick start script to prepare environment for running server for ABACUS agent tools and start an agent. `conda`, `abacus` and `bader` executable are required to use all functions.

Execute `quick_start.sh` and follow provided instructions to start the server and agent.