[project]
name = "py-mdr"
version = "0.7.0"
description = "Project to help interacting with the internal MDR project."
authors = [{ name = "Jorge L. Martinez", email = "jmartinez@schubergphilis.com" }]
requires-python = ">=3.10"
readme = "README.md"
dependencies = [
    "httpx~=0.28.1",
    "requests~=2.32.5",
]

[dependency-groups]
dev = [
    "pytest~=9.0.2",
    "pytest-cov~=7.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
