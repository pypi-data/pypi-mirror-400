[tool.poetry]
name = "drf-captcha"
version = "0.2.1"
description = "A - hopefully maintained - fork of django-simple-captcha to keep up with upgrading dependencies"
readme = "README.md"
authors = ["evgeny.zuev <zueves@gmail.com>"]
maintainers = ["omeraksoy <omer.aksoy@lifemote.com>"]
license = "MIT"
packages = [
    {include = "rest_captcha"},
]

[tool.poetry.dependencies]
python = "^3.8"
djangorestframework = "^3"
django = "<6.0"
Pillow = "*"

[tool.poetry.dev-dependencies]
tox = "^3.5"
