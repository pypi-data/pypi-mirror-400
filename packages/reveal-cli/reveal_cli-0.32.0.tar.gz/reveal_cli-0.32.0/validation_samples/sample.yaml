# Sample YAML configuration
name: test-config
version: 1.0.0

database:
  host: localhost
  port: 5432
  name: testdb

services:
  - name: web
    port: 8080
  - name: api
    port: 3000

features:
  logging: true
  caching: false
  metrics: true
