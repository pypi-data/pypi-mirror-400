# Union SDK

The SDK for [Union.ai](https://www.union.ai/).

## Installation

Install `flyteplugins-union`:

```bash
pip install flyteplugins-union
```

## Usage

```python
from flyteplugins.union.remote import ApiKey

# Create an API key
api_key = ApiKey.create(name="my-key")
```
