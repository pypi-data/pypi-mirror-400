[pytest]
markers =
    integration: Integration tests that require real API keys and use real connections

filterwarnings =
    ignore::bs4.builder.XMLParsedAsHTMLWarning
    ignore::DeprecationWarning

testpaths = tests
python_files = test_*.py
python_functions = test_*

addopts = -v --tb=short
