"""Helper functions for testing ai-jup syntax."""

def xq7(x: int, y: int) -> int:
    """
    Compute the sum of two integers and multiply by 2.
    
    Args:
        x: First integer
        y: Second integer
        
    Returns:
        The result of (x + y) * 2
        
    Example:
        >>> xq7(3, 5)
        16
    """
    return (x + y) * 2
