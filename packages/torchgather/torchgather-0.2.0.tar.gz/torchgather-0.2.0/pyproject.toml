[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "torchgather"
version = "0.2.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
]
