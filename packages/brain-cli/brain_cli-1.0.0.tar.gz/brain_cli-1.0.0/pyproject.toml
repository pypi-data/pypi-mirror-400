[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brain-cli"
version = "1.0.0"
description = "Assistente de Comandos com IA"
dependencies = [
    "typer",
    "rich",
    "pyperclip",
    "google-generativeai"
]

[project.scripts]
brain = "brain.main:app"