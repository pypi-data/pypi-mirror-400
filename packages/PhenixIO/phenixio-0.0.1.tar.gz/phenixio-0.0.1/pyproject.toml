[build-system]
requires = [
    "pdm-backend >= 2.4.0",
]
build-backend = "pdm.backend"

[project]
name = "PhenixIO"
version = "0.0.1"
authors = [
    { name = "Jian Wei Tay", email = "jianweitay@outlook.com" },
]
description = "Read images from Revvity (formerly PerkinElmer) Opera Phenix High Content Imager."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = [
    "LICEN[CS]E*",
]

[project.urls]
Homepage = "https://github.com/BioimageAnalysis/PhenixIO"
Issues = "https://github.com/BioimageAnalysis/PhenixIO/issues"

[tool.pytest.ini_options]
pythonpath = [
    "src",
]
