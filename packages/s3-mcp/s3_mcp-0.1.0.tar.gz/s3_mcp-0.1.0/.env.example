# AWS S3 MCP Server Configuration
# Copy this file to .env and configure your settings

# AWS Region (default: us-east-1)
# The AWS region where your S3 buckets are located
AWS_REGION=us-east-1

# AWS Profile (optional)
# If you use named profiles in ~/.aws/credentials, specify the profile name
# Leave commented to use the default profile
# AWS_PROFILE=my-profile

# Maximum Presigned URL Expiration (optional, default: 604800 seconds = 7 days)
# Controls the maximum expiration time for presigned URLs
# Valid range: 1 to 604800 seconds (1 second to 7 days)
# MAX_PRESIGNED_URL_EXPIRATION=604800

# ============================================================================
# AWS Credentials Configuration
# ============================================================================
#
# This MCP server uses boto3, which supports multiple authentication methods:
#
# 1. AWS CLI Configuration (Recommended for development)
#    - Run: aws configure
#    - Credentials stored in: ~/.aws/credentials
#    - Config stored in: ~/.aws/config
#
# 2. Environment Variables
#    - AWS_ACCESS_KEY_ID=your-access-key
#    - AWS_SECRET_ACCESS_KEY=your-secret-key
#    - AWS_SESSION_TOKEN=your-session-token (for temporary credentials)
#
# 3. IAM Roles (Recommended for production on AWS)
#    - Automatic when running on EC2, ECS, Lambda, etc.
#    - No credentials needed in .env
#
# 4. AWS SSO
#    - Run: aws sso login
#    - Configure AWS_PROFILE above
#
# For security, NEVER commit credentials to version control.
# Use .env (which is gitignored) or AWS credential files.
