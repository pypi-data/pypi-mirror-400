# Exclude specific files
# All files which are tracked by git and not explicitly excluded here are included by setuptools_scm
# Prune folders
prune build
prune docs/_build
prune docs/api
prune notebooks
prune hwo_disra/disra_notebooks
global-exclude *.pyc *.o
global-exclude *.ipynb
# But include test notebooks specifically
recursive-include hwo_disra/tests/resources *.ipynb
