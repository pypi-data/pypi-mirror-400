[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bequests"
version = "0.1.0"
description = "Stealth HTTP Engine for Python"
readme = "README.md"
authors = [{ name = "PainDe0Mie" }]
dependencies = [
    "curl_cffi",
]