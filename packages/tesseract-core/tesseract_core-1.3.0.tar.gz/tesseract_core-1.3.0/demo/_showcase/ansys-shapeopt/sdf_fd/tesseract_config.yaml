name: sdf_fd
version: "0.2.0"
description: |
  Higher-order Tesseract that takes a .stl-generating Tesseract reference
  and uses finite difference method to compute gradients for a signed distance field.

build_config:
  extra_packages:
    - "build-essential"
    - "libgl1"
