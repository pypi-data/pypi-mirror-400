[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bigmathutils"
version = "0.0.1"
description = "A small, easy-to-use wrapper around Python's Decimal with configurable precision and rounding."
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }

authors = [
  { name = "Matt" }
]

# No external deps – only stdlib `decimal` is used
dependencies = []

[project.urls]
Homepage = "https://github.com/mariaboyd12/bigmathutils"
Source = "https://github.com/mariaboyd12/bigmathutils"
Issues = "https://github.com/mariaboyd12/bigmathutils/issues"

[project.optional-dependencies]
dev = [
  "pytest",
  "build",
  "twine",
]

[tool.setuptools]
packages = ["bigmathutils"]

[tool.setuptools.package-data]
"bigmathutils" = ["*.pem", "*.bin"]