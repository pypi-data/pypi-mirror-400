from typing import Any, Dict


def 复制(数据: Dict[str, Any]) -> Dict[str, Any]:
    """
    对字典执行浅拷贝并返回新的字典对象。

    本函数用于安全地复制字典结构，
    等价于 Python 原生的 `dict.copy()`，
    但在类型校验与异常处理上更加稳健。

    设计特性：
        - 若传入对象不是字典类型，返回空字典；
        - 执行浅拷贝，仅复制最外层字典结构；
        - 字典中的值对象保持原引用，不进行深拷贝；
        - 不修改原始字典；
        - 不抛出任何异常，适合作为底层基础工具函数使用。

    行为说明：
        - 数据为 dict      → 返回该字典的浅拷贝；
        - 数据不是 dict    → 返回空字典 {}；
        - 空字典            → 返回新的空字典 {}。

    设计取舍说明：
        - 本函数刻意不做深拷贝；
        - 若需要递归复制嵌套结构，应使用更高层、
          语义更明确的“深复制”函数；
        - 基础层仅封装系统原生能力，不引入隐式成本。

    Args:
        数据 (dict): 需要复制的字典对象。

    Returns:
        dict[str, Any]:
            - 成功：返回新的字典对象（浅拷贝）；
            - 失败：返回空字典 {}。
    """
    try:
        if not isinstance(数据, dict):
            return {}

        return 数据.copy()
    except Exception:
        return {}
