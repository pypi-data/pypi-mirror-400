# Cursor rules (short + hard)
Read `AGENT.md` first. Follow Slice Protocol + Fail-fast + Evidence.
After each slice, run:
- ./scripts/run_with_log.sh verify_fast -- ./scripts/verify_fast.sh
Report: run_id + logs/latest.log + verify PASS/FAIL.
