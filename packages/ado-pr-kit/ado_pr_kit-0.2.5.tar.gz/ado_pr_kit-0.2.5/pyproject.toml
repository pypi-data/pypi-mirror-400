[project]
name = "ado-pr-kit"
version = "0.2.5"
readme = "README.md"
description = "AdoPRKit: The complete toolbox for Azure DevOps Pull Requests (MCP)"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "requests",
    "pydantic",
    "typer",
    "python-dotenv",
    "pytest",
    "pytest-mock",
    "black",
    "flake8",
    "pre-commit",
    "build",
    "twine",
    "fastapi",
    "uvicorn",
    "httpx"
]

[project.urls]
Repository = "https://github.com/om-surushe/AdoPRKit"
Homepage = "https://github.com/om-surushe/AdoPRKit"

[project.scripts]
ado-pr-kit = "ado_pr_kit.server:main"
ado-pr-connect = "ado_pr_kit.connect:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
