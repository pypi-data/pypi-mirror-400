// Clean JavaScript code without AI slop
function processData() {
  const items = [1, 2, 3];
  return items.map(x => x * 2);
}

function sumArray(arr) {
  return arr.reduce((sum, val) => sum + val, 0);
}

const config = {
  name: "app",
  version: "1.0.0"
};

export { processData, sumArray, config };
