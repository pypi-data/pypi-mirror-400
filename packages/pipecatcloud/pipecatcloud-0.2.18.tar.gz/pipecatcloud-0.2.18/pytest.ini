[tool:pytest]
# Basic pytest configuration

testpaths = tests
python_files = test_*.py
# Modern import mode for better import control with src/ layout
addopts = --import-mode=importlib

# Async support for CLI command tests
asyncio_mode = auto

# Markers actually used in tests
markers =
    asyncio: mark test as async test