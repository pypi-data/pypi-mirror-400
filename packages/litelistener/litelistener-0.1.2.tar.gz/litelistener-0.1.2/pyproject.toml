[project]
name = "litelistener"
version = "0.1.2"
description = "Uses liteclient to listen for keys in a redis-like queue"
readme = "README.md"
authors = [
    { name = "Joe Pasquantonio", email = "joe.pasquantonio@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
	"liteclient",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
