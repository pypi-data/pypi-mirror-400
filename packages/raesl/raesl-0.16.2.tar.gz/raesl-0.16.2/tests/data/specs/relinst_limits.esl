define type
    limited is a real of at most 1.0

define relation
    myfunc
        requiring parameters
            * p is a real
        relating parameters
            * q is a real
        returning parameters
            * r is a real

world
    variable
        lim is a limited

    relations
        mf: myfunc
            requiring arguments
                * lim  # OK
            relating arguments
                * lim
            returning arguments
                * lim
