syntax = "proto3";

package strique.model.widget.tiktok;

import "constants/ui/render_type.proto";
import "constants/ui/sorting_direction.proto";
import "options/strique_enum_options.proto";
import "types/currency/currency.proto";
import "types/filter_predicate/filters.proto";

option java_outer_classname = "TikTokAdsTrendAnalysisProto";
option java_package = "com.strique.model.widget.tiktok";

// Next Id: 6
// Proto definition for TikTok Ads Trend Analysis Data.
message TikTokAdsTrendAnalysisData {
  // Title for this widget.
  string widget_title = 1;

  // List of records for this widget.
  repeated TiktTokAdsTrendAnalysisRecord records = 2;

  // Total record.
  optional TiktTokAdsTrendAnalysisRecord total = 3;

  // Summary data for a subset of records.
  optional TiktTokAdsTrendAnalysisRecord subtotal = 4;

  //overview message if widget has different currencies.
  optional string overview_message = 5;
}

// Next Id: 64
// Proto definition for a single record of TikTok Ads Trend Analysis.
message TiktTokAdsTrendAnalysisRecord {
  // date for this record
  string formatted_date = 63 [json_name = "formattedDate"];

  // Cost.
  // Amount spent on advertising.
  types.Currency cost = 2 [json_name = "cost"];

  // CPC (Destination).
  // Average cost for each click (destionation) on the ad.
  types.Currency cost_per_click_destination = 3 [json_name = "costPerClickDestination"];

  // CPM.
  // Cost of 1000 impressions.
  types.Currency cost_per_mille = 4 [json_name = "costPerMille"];

  // Impression.
  int64 impression = 5 [json_name = "impression"];

  // Clicks (destination).
  // Number of clicks (destination).
  int64 clicks_destination = 6 [json_name = "clicksDestination"];

  // CTR (destination).
  // Percentage of impressions resulting in a click.
  float click_through_rate_destination = 7 [json_name = "clickThroughRateDestination"];

  // Reach.
  int64 reach = 8 [json_name = "reach"];

  // Frequency.
  // Total impressions per reach.
  float frequency = 9 [json_name = "frequency"];

  // Conversion.
  // Number of conversions.
  int64 conversion = 10 [json_name = "conversion"];

  // Cost per conversion.
  // Amount spent per conversion.
  types.Currency cost_per_conversion = 11 [json_name = "costPerConversion"];

  // Conversion rate (CVR).
  // Percentage of clicks on the ad that resulted in a conversion.
  float conversion_rate = 12 [json_name = "conversionRate"];

  // Clicks (all).
  // Total number of clicks (all).
  int64 clicks_all = 13 [json_name = "clicksAll"];

  // CTR (all).
  // Percentage of impressions resulting in a click.
  float click_through_rate_all = 14 [json_name = "clickThroughRateAll"];

  // Complete payment ROAS.
  // Return on ad spend for complete payments.
  double complete_payment_roas = 15 [json_name = "completePaymentRoas"];

  // Payments completed (page).
  // Number of completed payments (page).
  int64 payments_completed_page = 16 [json_name = "paymentsCompletedPage"];

  // Cost per payment completed (page).
  // Amount spent per completed payment (page).
  types.Currency cost_per_payment_completed_page = 17 [json_name = "costPerPaymentCompletedPage"];

  // Value per complete payment (page).
  // Value received per completed payment (page).
  types.Currency value_per_complete_payment_page = 18 [json_name = "valuePerCompletePaymentPage"];

  // Payment completion value.
  // Total value from payment completions.
  types.Currency payment_completion_value = 19 [json_name = "paymentCompletionValue"];

  // Add to cart (page).
  // Number of add-to-cart actions (page).
  int64 add_to_cart_page = 20 [json_name = "addToCartPage"];

  // Cost per add to cart (page).
  // Amount spent per add-to-cart action (page).
  types.Currency cost_per_add_to_cart_page = 21 [json_name = "costPerAddToCartPage"];

  // Add to cart value (page).
  // Total value from add-to-cart actions (page).
  types.Currency add_to_cart_value_page = 22 [json_name = "addToCartValuePage"];

  // Checkout initiated (page).
  // Number of checkouts initiated (page).
  int64 checkout_initiated_page = 23 [json_name = "checkoutInitiatedPage"];

  // Cost per checkout initiated (page).
  // Amount spent per initiated checkout (page).
  types.Currency cost_per_checkout_initiated_page = 24 [json_name = "costPerCheckoutInitiatedPage"];

  // Checkout initiated value (page).
  // Value from initiated checkouts (page).
  types.Currency checkout_initiated_value_page = 25 [json_name = "checkoutInitiatedValuePage"];

  // Subscribe (page).
  // Number of subscriptions (page).
  int64 subscribe_page = 26 [json_name = "subscribePage"];

  // Cost per subscribe (page).
  // Amount spent per subscription (page).
  types.Currency cost_per_subscribe_page = 27 [json_name = "costPerSubscribePage"];

  // Value per subscribe (page).
  // Value per subscription (page).
  types.Currency value_per_subscribe_page = 28 [json_name = "valuePerSubscribePage"];

  // Subscribe value (page).
  // Total value from subscriptions (page).
  types.Currency subscribe_value_page = 29 [json_name = "subscribeValuePage"];

  // Form submission (page).
  // Number of form submissions (page).
  int64 form_submission_page = 30 [json_name = "formSubmissionPage"];

  // Cost per submitted (page).
  // Amount spent per form submission (page).
  types.Currency cost_per_submitted_page = 31 [json_name = "costPerSubmittedPage"];

  // Value per form submit (page).
  // Value per form submission (page).
  types.Currency value_per_form_submit_page = 32 [json_name = "valuePerFormSubmitPage"];

  // Form submission value (page).
  // Total value from form submissions (page).
  types.Currency form_submission_value_page = 33 [json_name = "formSubmissionValuePage"];
  
  // Video views
  // total number of video views.
  int64 video_views = 34 [json_name = "videoViews"];

  // 2-second Video views
  // number of 2-second video views.
  int64 video_views_2_seconds = 35 [json_name = "videoViews2Seconds"];

  // 6-second Video views
  // number of 6-second video views.
  int64 video_views_6_seconds = 36 [json_name = "videoViews6Seconds"];

  // 6-second focused views
  // number of 6-second focused video views.
  int64 video_views_6_seconds_focused = 37 [json_name = "videoViews6SecondsFocused"];

  // Video views at 100%
  // number of video views watched to completion (100%).
  int64 video_views_100_percent = 38 [json_name = "videoViews100Percent"];

  // Video views at 75%
  // number of video views watched up to 75%.
  int64 video_views_75_percent = 39 [json_name = "videoViews75Percent"];

  // Video views at 50%
  // number of video views watched up to 50%.
  int64 video_views_50_percent = 40 [json_name = "videoViews50Percent"];

  // Video views at 25%
  // number of video views watched up to 25%.
  int64 video_views_25_percent = 41 [json_name = "videoViews25Percent"];

  // Average play time per video view
  // average duration of play time per video view.
  float average_play_time_per_video_view = 42 [json_name = "averagePlayTimePerVideoView"];

  // Average play time per user
  // average duration of play time per user.
  float average_play_time_per_user = 43 [json_name = "averagePlayTimePerUser"];

  // Paid follows
  // number of paid follows.
  int64 paid_follows = 44 [json_name = "paidFollows"];

  // Paid likes
  // number of paid likes.
  int64 paid_likes = 45 [json_name = "paidLikes"];

  // Paid comments
  // number of paid comments.
  int64 paid_comments = 46 [json_name = "paidComments"];

  // Paid shares
  // number of paid shares.
  int64 paid_shares = 47 [json_name = "paidShares"];

  // Unique payment completion rate % (page)
  // percentage of unique payments completed (page).
  float unique_payment_completion_rate = 48 [json_name = "uniquePaymentCompletionRate"];

  // Total landing page views
  // total number of landing page views.
  int64 total_landing_page_views = 49 [json_name = "totalLandingPageViews"];

  // Cost per landing page view
  // amount spent per landing page view.
  types.Currency cost_per_landing_page_view = 50 [json_name = "costPerLandingPageView"];

  // Landing page view rate
  // percentage of visitors viewing the landing page.
  float landing_page_view_rate = 51 [json_name = "landingPageViewRate"];

  // Total page view
  // total number of page views.
  int64 total_page_view = 52 [json_name = "totalPageView"];

  // Cost per page view
  // amount spent per page view.
  types.Currency cost_per_page_view = 53 [json_name = "costPerPageView"];

  // Page view rate %
  // percentage of visitors viewing the page.
  float page_view_rate = 54 [json_name = "pageViewRate"];

  // Value per page view
  types.Currency value_per_page_view = 55 [json_name = "valuePerPageView"];

  // Total page view value
  // total value from page views.
  types.Currency total_page_view_value = 56 [json_name = "totalPageViewValue"];

  // Unique add to cart rate % (page)
  // percentage of unique add-to-cart actions (page).
  float unique_add_to_cart_rate = 57 [json_name = "uniqueAddToCartRate"];

  // Value per add to cart (page)
  // value per add-to-cart action (page).
  types.Currency value_per_add_to_cart_page = 58 [json_name = "valuePerAddToCartPage"];

  // Unique checkout initiated % (page)
  // percentage of unique checkouts initiated (page).
  float unique_checkout_initiated_rate = 59 [json_name = "uniqueCheckoutInitiatedRate"];

  // Value per checkout initiated (page)
  types.Currency value_per_checkout_initiated_page = 60 [json_name = "valuePerCheckoutInitiatedPage"];

   // Unique subscribe rate % (page)
  // percentage of unique subscriptions (page).
  float unique_subscribe_rate = 61 [json_name = "uniqueSubscribeRate"];

  // Unique form submission rate % (page)
  // percentage of unique form submissions (page).
  float unique_form_submission_rate = 62 [json_name = "uniqueFormSubmissionRate"];

  reserved 1;
}

// Next Id: 4
// Proto definition for TikTok Ads Trend Analysis Configuration.
message TikTokAdsTrendAnalysisConfig {
  // Next Id: 63
  // Enumeration of possible columns in the TikTok Ads Trend Analysis table.
  enum Column {
    COLUMN_UNKNOWN = 0 [
      (options.label) = 'Unknown',
      (options.accessor_key) = '',
      (options.is_sortable) = false,
      (options.is_filterable) = false,
      (options.is_dropdown_not_support) = true
    ];
    COLUMN_DATE = 1 [
      (options.label) = 'Date',
      (options.accessor_key) = 'formattedDate',
      (options.is_sortable) = true,
      (options.is_filterable) = false,
      (options.is_dropdown_not_support) = true
    ];
    COLUMN_COST = 2 [
      (options.label) = 'Cost',
      (options.accessor_key) = 'cost',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CLICK_DESTINATION = 3 [
      (options.label) = 'Cost per click (destination)',
      (options.accessor_key) = 'costPerClickDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_MILLE = 4 [
      (options.label) = 'CPM',
      (options.accessor_key) = 'costPerMille',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_IMPRESSION = 5 [
      (options.label) = 'Impression',
      (options.accessor_key) = 'impression',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICKS_DESTINATION = 6 [
      (options.label) = 'Clicks (destination)',
      (options.accessor_key) = 'clicksDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICK_THROUGH_RATE_DESTINATION = 7 [
      (options.label) = 'Click-through rate (destination)',
      (options.accessor_key) = 'clickThroughRateDestination',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_REACH = 8 [
      (options.label) = 'Reach',
      (options.accessor_key) = 'reach',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FREQUENCY = 9 [
      (options.label) = 'Frequency',
      (options.accessor_key) = 'frequency',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONVERSION = 10 [
      (options.label) = 'Conversion',
      (options.accessor_key) = 'conversion',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CONVERSION = 11 [
      (options.label) = 'Cost per conversion',
      (options.accessor_key) = 'costPerConversion',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CONVERSION_RATE = 12 [
      (options.label) = 'Conversion rate (CVR)',
      (options.accessor_key) = 'conversionRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_CLICKS_ALL = 13 [
      (options.label) = 'Clicks (all)',
      (options.accessor_key) = 'clicksAll',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CLICK_THROUGH_RATE_ALL = 14 [
      (options.label) = 'Click-through rate (all)',
      (options.accessor_key) = 'clickThroughRateAll',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_COMPLETE_PAYMENT_ROAS = 15 [
      (options.label) = 'Complete payment ROAS',
      (options.accessor_key) = 'completePaymentRoas',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = 'X'
    ];
    COLUMN_PAYMENTS_COMPLETED_PAGE = 16 [
      (options.label) = 'Payments completed (page)',
      (options.accessor_key) = 'paymentsCompletedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_PAYMENT_COMPLETED_PAGE = 17 [
      (options.label) = 'Cost per payment completed (page)',
      (options.accessor_key) = 'costPerPaymentCompletedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VALUE_PER_COMPLETE_PAYMENT_PAGE = 18 [
      (options.label) = 'Value per complete payment (page)',
      (options.accessor_key) = 'valuePerCompletePaymentPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_PAYMENT_COMPLETION_VALUE = 19 [
      (options.label) = 'Payment completion value',
      (options.accessor_key) = 'paymentCompletionValue',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_ADD_TO_CART_PAGE = 20 [
      (options.label) = 'Add to cart (page)',
      (options.accessor_key) = 'addToCartPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_ADD_TO_CART_PAGE = 21 [
      (options.label) = 'Cost per add to cart (page)',
      (options.accessor_key) = 'costPerAddToCartPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_ADD_TO_CART_VALUE_PAGE = 22 [
      (options.label) = 'Add to cart value (page)',
      (options.accessor_key) = 'addToCartValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CHECKOUT_INITIATED_PAGE = 23 [
      (options.label) = 'Checkout initiated (page)',
      (options.accessor_key) = 'checkoutInitiatedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_CHECKOUT_INITIATED_PAGE = 24 [
      (options.label) = 'Cost per checkout initiated (page)',
      (options.accessor_key) = 'costPerCheckoutInitiatedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_CHECKOUT_INITIATED_VALUE_PAGE = 25 [
      (options.label) = 'Checkout initiated value (page)',
      (options.accessor_key) = 'checkoutInitiatedValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_SUBSCRIBE_PAGE = 26 [
      (options.label) = 'Subscribe (page)',
      (options.accessor_key) = 'subscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_SUBSCRIBE_PAGE = 27 [
      (options.label) = 'Cost per subscribe (page)',
      (options.accessor_key) = 'costPerSubscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VALUE_PER_SUBSCRIBE_PAGE = 28 [
      (options.label) = 'Value per subscribe (page)',
      (options.accessor_key) = 'valuePerSubscribePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_SUBSCRIBE_VALUE_PAGE = 29 [
      (options.label) = 'Subscribe value (page)',
      (options.accessor_key) = 'subscribeValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FORM_SUBMISSION_PAGE = 30 [
      (options.label) = 'Form submission (page)',
      (options.accessor_key) = 'formSubmissionPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_SUBMITTED_PAGE = 31 [
      (options.label) = 'Cost per submitted (page)',
      (options.accessor_key) = 'costPerSubmittedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VALUE_PER_FORM_SUBMIT_PAGE = 32 [
      (options.label) = 'Value per form submit (page)',
      (options.accessor_key) = 'valuePerFormSubmitPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_FORM_SUBMISSION_VALUE_PAGE = 33 [
      (options.label) = 'Form submission value (page)',
      (options.accessor_key) = 'formSubmissionValuePage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_VIDEO_VIEWS = 34 [
      (options.label) = 'Video views',
      (options.accessor_key) = 'videoViews',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_2_SECONDS = 35 [
      (options.label) = '2-second video views',
      (options.accessor_key) = 'videoViews2Seconds',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_6_SECONDS = 36 [
      (options.label) = '6-second video views',
      (options.accessor_key) = 'videoViews6Seconds',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_6_SECONDS_FOCUSED = 37 [
      (options.label) = '6-second focused views',
      (options.accessor_key) = 'videoViews6SecondsFocused',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_100_PERCENT = 38 [
      (options.label) = 'Video views at 100%',
      (options.accessor_key) = 'videoViews100Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_75_PERCENT = 39 [
      (options.label) = 'Video views at 75%',
      (options.accessor_key) = 'videoViews75Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_50_PERCENT = 40 [
      (options.label) = 'Video views at 50%',
      (options.accessor_key) = 'videoViews50Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_VIDEO_VIEWS_25_PERCENT = 41 [
      (options.label) = 'Video views at 25%',
      (options.accessor_key) = 'videoViews25Percent',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_AVERAGE_PLAY_TIME_PER_VIDEO_VIEW = 42 [
      (options.label) = 'Average play time per video view',
      (options.accessor_key) = 'averagePlayTimePerVideoView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_AVERAGE_PLAY_TIME_PER_USER = 43 [
      (options.label) = 'Average play time per user',
      (options.accessor_key) = 'averagePlayTimePerUser',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_PAID_FOLLOWS = 44 [
      (options.label) = 'Paid follows',
      (options.accessor_key) = 'paidFollows',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_PAID_LIKES = 45 [
      (options.label) = 'Paid likes',
      (options.accessor_key) = 'paidLikes',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_PAID_COMMENTS = 46 [
      (options.label) = 'Paid comments',
      (options.accessor_key) = 'paidComments',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];    
    COLUMN_PAID_SHARES = 47 [
      (options.label) = 'Paid shares',
      (options.accessor_key) = 'paidShares',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_PAYMENT_COMPLETION_RATE = 48 [
      (options.label) = 'Unique payment completion rate % (page)',
      (options.accessor_key) = 'uniquePaymentCompletionRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_TOTAL_LANDING_PAGE_VIEWS = 49 [
      (options.label) = 'Total landing page views',
      (options.accessor_key) = 'totalLandingPageViews',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_LANDING_PAGE_VIEW = 50 [
      (options.label) = 'Cost per landing page view',
      (options.accessor_key) = 'costPerLandingPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_LANDING_PAGE_VIEW_RATE = 51 [
      (options.label) = 'Landing page view rate',
      (options.accessor_key) = 'landingPageViewRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_TOTAL_PAGE_VIEWS = 52 [
      (options.label) = 'Total page view',
      (options.accessor_key) = 'totalPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_COST_PER_PAGE_VIEW = 53 [
      (options.label) = 'Cost per page view',
      (options.accessor_key) = 'costPerPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_PAGE_VIEW_RATE = 54 [
      (options.label) = 'Page view rate %',
      (options.accessor_key) = 'pageViewRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_PAGE_VIEW = 55 [
      (options.label) = 'Value per page view',
      (options.accessor_key) = 'valuePerPageView',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_TOTAL_PAGE_VIEW_VALUE = 56 [
      (options.label) = 'Total page view value',
      (options.accessor_key) = 'totalPageViewValue',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_ADD_TO_CART_RATE = 57 [
      (options.label) = 'Unique add to cart rate % (page)',
      (options.accessor_key) = 'uniqueAddToCartRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_ADD_TO_CART_PAGE = 58 [
      (options.label) = 'Value per add to cart (page)',
      (options.accessor_key) = 'valuePerAddToCartPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_CHECKOUT_INITIATED_RATE = 59 [
      (options.label) = 'Unique checkout initiated % (page)',
      (options.accessor_key) = 'uniqueCheckoutInitiatedRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
    COLUMN_VALUE_PER_CHECKOUT_INITIATED_PAGE = 60 [
      (options.label) = 'Value per checkout initiated (page)',
      (options.accessor_key) = 'valuePerCheckoutInitiatedPage',
      (options.is_sortable) = true,
      (options.is_filterable) = true
    ];
    COLUMN_UNIQUE_SUBSCRIBE_RATE = 61 [
      (options.label) = 'Unique subscribe rate % (page)',
      (options.accessor_key) = 'uniqueSubscribeRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];   
    COLUMN_UNIQUE_FORM_SUBMISSION_RATE = 62 [
      (options.label) = 'Unique form submission rate % (page)',
      (options.accessor_key) = 'uniqueFormSubmissionRate',
      (options.is_sortable) = true,
      (options.is_filterable) = true,
      (options.suffix) = '%'
    ];
  }

  // Next Id: 8
  // Configuration for the table displayed in the TikTok Ads Trend Analysis.
  message TableConfig {
    // Specifies the order of columns in the table.
    repeated Column column_order = 1;

    // Specifies columns to be hidden in the table.
    repeated Column column_hidden = 2;

    // Specifies the default column used for sorting.
    Column default_sorted_column = 3;

    // Specifies the sorting direction.
    constants.ui.SortingDirection sorting_direction = 4;

    // Indicates whether to display the total row in the table.
    bool show_total = 5;

    // Indicates whether to display the subtotal row in the table.
    bool show_subtotal = 6;

    types.Expression filter_expression = 7;
  }

  // Next Id: 3
  // Configuration for the area chart displayed.
  message AreaChartConfig {
    // Specifies the dimension for the area chart.
    Column dimension = 1;

    // Specifies the areas for the area chart.
    repeated Column areas = 2;
  }

  // Specifies the type of rendering for the widget.
  constants.ui.RenderType render_type = 1;

  // Configuration for the table displayed in the widget.
  TableConfig table_config = 2;

  // Configuration for the area chart displayed in the widget.
  AreaChartConfig area_chart_config = 3;
}
