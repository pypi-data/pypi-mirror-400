[project]
name = "envdo"
version = "0.1.1"
description = "为命令行程序配置临时的启动环境变量，特别适合为 claude code 配置切换模型。"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "rich>=14.2.0",
]

[project.urls]
repository = "https://github.com/NewToolAI/envdo"

[project.scripts]
envdo = "envdo.cli:main"
