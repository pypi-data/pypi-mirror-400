[project]
name = "restic-qdirstat"
version = "0.1.0"
description = "Analyze restic snapshots with QDirStat"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
restic-qdirstat = "restic_qdirstat:main"

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
