[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "email_management"
version = "0.1.0"
description = "API for managing your email"
requires-python = ">=3.8"
dependencies = [
    "pytest",
    "langchain",
    "langchain-core",
    "langchain-openai"
]

# 👇 IMPORTANT PART
[tool.hatch.build.targets.wheel]
packages = ["src/email_management"]
include = ["src/email_management/py.typed"]


