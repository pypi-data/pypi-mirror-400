[project]
name = "amsterdam-django-oidc"
version = "0.2.3"
description = "Extension of the authenctication backend of the mozilla-django-oidc package, with extra validation."
authors = [
    {name = "Nino van der Laan",email = "n.van.der.laan@amsterdam.nl"},
    {name = "Youri Westerman",email = "y.westerman@amsterdam.nl"},
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "mozilla-django-oidc (>=5.0.2,<6.0.0)"
]

[project.urls]
Repository = "http://github.com/Amsterdam/amsterdam-django-oidc.git"

[build-system]
requires = ["uv_build>=0.9.2,<1.0.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "django-stubs[compatible-mypy]>=5.2.7",
    "mypy>=1.14.1",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.0",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D100", "D101", "D102", "D103", "D104", "D205", "D400", "D415", "PT009"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S105"]

[tool.pytest]
django_find_project = false

[tool.mypy]
mypy_path = "$MYPY_CONFIG_FILE_DIR/stubs"
