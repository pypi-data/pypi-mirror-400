[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyuniverse"
version = "0.1.1"
description = "Create unique Identifiers with ease."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Thomas McCann"}
]

dependencies = []
