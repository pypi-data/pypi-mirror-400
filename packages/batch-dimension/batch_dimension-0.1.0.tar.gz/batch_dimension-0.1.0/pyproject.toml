[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "batch-dimension"
version = "0.1.0"
description = "batchdim"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
    { email = "hello@batchdim.com" }
]

[tool.hatch.build.targets.wheel]
packages = ["src/batchdim"]
