define verb
    convert to

define component X
    parameters
        a is a real
        b is an integer

    transformation-requirements
        g1: must convert a,b to b,a
        g1: must convert b to a

        g2: must convert a to b with subclause
            * s1: a could be at most t.b.d.
            * s1: b must be at least t.b.d.

define component Y
    variable
        a is a real
        b is an integer

    components
        x is an X with arguments
            * a, b

    transformation-requirements
        g2: must convert a to b with subclause
            * s1: a could be at most t.b.d.

    transformation-requirements
        g2: must convert a to b

world
    empty
