apiVersion: v1
kind: Service
metadata:
  name: mind-gardener
  namespace: mind
  labels:
    app: mind-gardener
    app.kubernetes.io/name: mind-gardener
    app.kubernetes.io/component: worker
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 9091
      targetPort: health
      protocol: TCP
  selector:
    app: mind-gardener
