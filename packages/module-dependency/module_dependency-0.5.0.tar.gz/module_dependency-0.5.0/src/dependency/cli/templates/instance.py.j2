from dependency.core import instance, providers
from {{ component.path }} import {{ component.interface }}, {{ component.name }}

@instance(
    component={{ component.name }},
    imports=[{% for import in instance.imports %}
        {{ import }},{% endfor %}
    ],
    provider=providers.Singleton,
)
class {{ instance.name }}({{ component.interface }}):
    def __init__(self) -> None:
        {% for import in instance.imports %}
        self.__{{ import.lower() }}: {{ import }} = {{ import }}.provide(){% endfor %}
        super().__init__()
    
    def method(self) -> None:
        pass