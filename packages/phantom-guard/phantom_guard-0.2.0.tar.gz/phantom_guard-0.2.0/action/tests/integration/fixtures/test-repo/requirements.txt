# Test requirements file for integration tests
# Contains mix of safe and suspicious packages

# Safe packages
flask==2.0.0
requests>=2.25.0
numpy

# Suspicious packages (should trigger signals)
flask-gpt-helper==1.0.0
easy-django==0.5.0
gpt-wrapper==0.1.0
