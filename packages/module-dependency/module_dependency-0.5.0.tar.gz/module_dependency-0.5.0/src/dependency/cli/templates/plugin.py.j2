from pydantic import BaseModel
from dependency.core import Plugin, PluginMeta, module

class {{ module.name }}Config(BaseModel):
    pass

@module()
class {{ module.name }}(Plugin):
    meta = PluginMeta(name="{{ module.name }}", version="0.0.1")

    def config(self) -> {{ module.name }}Config:
        return {{ module.name }}Config(**self.container.config())