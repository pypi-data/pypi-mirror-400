define relation
    myfunc
        requiring parameters
            * p is a real
            * q is a real
        returning parameters
            * one or more r is a real

world
    variable
        a is a real

    relations
        f1: myfunc
            requiring arguments
                * a,a
            returning arguments
                * a, a, a
