[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
 
[project]
name = "arq_base_python"
version = "1.0.1.0b2"
description = "Una libreria simple de python"
license = { file = "LICENSE" }
dependencies = []
requires-python = ">=3.8"
