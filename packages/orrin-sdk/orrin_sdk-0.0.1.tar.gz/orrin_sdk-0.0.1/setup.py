from setuptools import setup, find_packages

setup(
    name="orrin-sdk",
    version="0.0.1",
    description="Orrin SDK for developers",
    packages=find_packages(),
    python_requires=">=3.9",
)
