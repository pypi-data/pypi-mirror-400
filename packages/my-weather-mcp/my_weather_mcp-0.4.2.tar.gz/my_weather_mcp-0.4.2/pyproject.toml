[project]
name = "my-weather-mcp"
version = "0.4.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BasimahLY", email = "934796059@qq.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
]

[project.scripts]
my-weather-mcp = "my_weather_mcp:main"

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
