# Test edge cases for continuation lines and variable expansion

# Commented continuation line
#GRUB_CMDLINE_LINUX_DEFAULT="commented \
#continuation"

# Regular param
GRUB_TIMEOUT=5

# Continuation with extra spaces/tabs
GRUB_DISTRIBUTOR="Arch \
    Linux \
    Distribution"

# Single line (no continuation)
GRUB_DISABLE_RECOVERY=true

# Variable expansion with continuation (should blacklist)
GRUB_FOO="${GRUB_TIMEOUT} \
extra_value"

# Another blacklisted reference
GRUB_BAR="$GRUB_FOO test"
