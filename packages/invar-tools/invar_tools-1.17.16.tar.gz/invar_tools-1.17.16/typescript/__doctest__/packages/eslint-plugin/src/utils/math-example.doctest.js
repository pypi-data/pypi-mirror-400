/**
 * Auto-generated doctest file from packages/eslint-plugin/src/utils/math-example.ts
 * DO NOT EDIT - Generated by generate-doctests.mjs
 */

import { describe, it, expect } from 'vitest';
import * as sourceModule from '../../../../../packages/eslint-plugin/src/utils/math-example';

// Extract exported functions
const { multiply, add } = sourceModule;

describe('multiply @example tests', () => {
  it('should work as documented', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(multiply(2, 3)).toEqual(6);
  });

  it('should work as documented (example 2)', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(multiply(0, 100)).toEqual(0);
  });

  it('should work as documented (example 3)', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(multiply(-2, 5)).toEqual(-10);
  });

});

describe('add @example tests', () => {
  it('should work as documented', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(add(2, 3)).toEqual(5);
  });

  it('should work as documented (example 2)', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(add(-1, 1)).toEqual(0);
  });

  it('should work as documented (example 3)', async () => {
    // Source: packages/eslint-plugin/src/utils/math-example.ts
    expect(add(2, 2)).toEqual(4);
  });

});

