# github_image.yaml

body:
  name: github-image
  version: 0.0.1-test
  description: Creating a new github type image
  type: workflow
  buildDetails:
    source: github
    repository: "https://example.com"
    branch: main
    token: token
    dockerfilePath: Dockerfile
    buildArguments:
      - name: PARAM1
        value: param1
      - name: PARAM2
        value: param2
    secrets:
      - SECRET1
      - SECRET2
    useCache: true
