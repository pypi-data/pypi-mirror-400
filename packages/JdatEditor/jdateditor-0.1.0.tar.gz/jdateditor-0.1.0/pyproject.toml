[project]
name = "JdatEditor"
version = "0.1.0"
description = "Module minimaliste pour lire et éditer des fichiers JDAT"
authors = [
    { name = "GabiMinecraft02", email = "gabriel.m020913@gmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8"
keywords = ["JDAT", "editor", "data"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["JdatEditor"]
include-package-data = true
