syntax = "proto3";

package compas_pb.data;

message PointData {
  // no inheritance, so Data will have to repeate but apparently
  string guid = 1;
  string name = 2;
  float x = 3;
  float y = 4;
  float z = 5;
}
