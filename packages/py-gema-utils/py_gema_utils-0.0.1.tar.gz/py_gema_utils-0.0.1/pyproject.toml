[tool.poetry]
name = "py-gema-utils"
version = "0.0.1"
description = ""
authors = ["Alexandru Stan <astan@it4ipm.de>"]
readme = "README.md"
packages = [
    {include = "py_gema_utils"},
]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
