[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "grapho-mathcore"
version = "0.1.0"
description = "Safe mathematical evaluation and utilities"
authors = [{ name="Lynx" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.1"

dependencies = []

