[build-system]
requires = ["setuptools>=61.0"]
build-backend = 'setuptools.build_meta'

[tool.setuptools]
packages = ["ethz_iam_webservice"]

[project]
name="ethz_iam_webservice"
version="0.16.10"
authors=[{ name="Swen Vermeul", email="swen@ethz.ch" }]
description="Manage users, groups and services of the ETH Identity and Access Management system (IAM)"
readme = "README.md"
dependencies=[
    "requests",
    "click>=7.0",
    "pyyaml",
    "python-dateutil",
    "pytz",
]
license={text="Apache Software License Version 2.0"}
requires-python = ">=3.9"
classifiers=[
    "Programming Language :: Python :: 3.9",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
[project.optional-dependencies]
dev = [
    "pytest"
]
[project.urls]
"Homepage"= "https://gitlab.ethz.ch/sis/tools/ethz-iam-webservice"
"Bug Tracker"="https://gitlab.ethz.ch/sis/tools/ethz-iam-webservice/-/issues"


[project.scripts]
nethz="ethz_iam_webservice.main:cli"
iam="ethz_iam_webservice.main:cli"
