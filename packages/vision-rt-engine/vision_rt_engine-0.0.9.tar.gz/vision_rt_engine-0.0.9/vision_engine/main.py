#!/usr/bin/env python

import torch

from vision_engine.build import *
from vision_engine.models.build_tracker import *

# if torch.multiprocessing.get_start_method(allow_none=True) != 'spawn':
#     torch.multiprocessing.set_start_method('spawn')
