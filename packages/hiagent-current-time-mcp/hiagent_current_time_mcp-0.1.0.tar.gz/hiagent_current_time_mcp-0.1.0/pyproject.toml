[project]
name = "hiagent-current-time-mcp"
version = "0.1.0"
description = "The MCP server to get local time"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.14.2",
]

[tool.uv.workspace]
members = ["venv"]

[project.scripts]
current-time-mcp = "current_time_mcp.server:main"
