[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ncert-dl"
version = "1.0.1"
description = "NCERT textbook downloader for Classes 6–12"
readme = "README.md"
license = "MIT" 
authors = [
  { name = "Parth Dalal" }
]
requires-python = ">=3.9"
dependencies = [
  "requests",
  "tqdm"
]

classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]


[project.scripts]
ncert-dl = "ncert_dl.cli:main"
