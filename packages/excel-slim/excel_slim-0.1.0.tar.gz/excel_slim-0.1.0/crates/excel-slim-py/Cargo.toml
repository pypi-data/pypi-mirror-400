[package]
name = "excel-slim-py"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
excel-slim-core = { path = "../excel-slim-core" }
pyo3 = { version = "0.21", features = ["extension-module"] }
serde_json = "1"
