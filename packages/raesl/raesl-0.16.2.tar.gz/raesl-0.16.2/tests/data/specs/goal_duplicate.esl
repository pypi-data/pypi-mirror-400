define verb
    convert to

define component X
    empty

world
    variable
        a is a real
        b is an integer

    components
        x is an X
        y is an X

    goal-requirements
        g1: x must convert a to y
        g1: y must convert b to x

        g2: x must convert a to y with subclause
            * s1: a could be at most t.b.d.
            * s1: b must be at least t.b.d.

    goal-requirements
        g2: x must convert a to y
