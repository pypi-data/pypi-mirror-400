Internaly this use `addons-installer` to find automatically addons path based on environment variable.

[NOTE]
====
If in the addons path a file named `EXCLUDE` exist, then this addons path won't be added to Odoo. +
But if you `{addons-installer}` to fetch and install it, then this addon-path will still exist on disk and installed.
====
