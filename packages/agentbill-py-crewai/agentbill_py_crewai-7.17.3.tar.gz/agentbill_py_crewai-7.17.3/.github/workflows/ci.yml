name: CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'sdks/crewai/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'sdks/crewai/**'
      - '.github/workflows/ci.yml'

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        python-version: ['3.12']
        os: [ubuntu-latest]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      
      - name: Run linting
        run: |
          pip install pylint
          pylint agentbill_crewai
      
      - name: Run tests
        run: |
          pytest --cov=agentbill_crewai --cov-report=xml --cov-report=term
      
      - name: Upload coverage
        if: matrix.python-version == '3.11' && matrix.os == 'ubuntu-latest'
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
      
      - name: Build package
        run: python -m build
