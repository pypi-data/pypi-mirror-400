id: configmap_integrate
title: "Deployment must mount ConfigMap created via template"
stack: helm
difficulty: Hard
xp: 150
goal: "Integrate a ConfigMap into a Helm chart and mount it correctly within a Kubernetes Deployment"
skills:
  - helm-templating
  - configmap-management
  - kubernetes-deployments
hint: "Create configmap.yaml template using {{ .Values.config }} and mount it in Deployment via volume+volumeMounts."
validator: "validator.py"
