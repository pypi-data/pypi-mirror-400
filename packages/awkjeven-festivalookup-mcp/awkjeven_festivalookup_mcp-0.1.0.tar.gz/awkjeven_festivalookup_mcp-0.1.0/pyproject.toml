[project]
name = "awkjeven-festivalookup-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.25.0",
]

[project.scripts]
awkjeven-festivalookup-mcp = "awkjeven_festivalookup_mcp:main"

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
