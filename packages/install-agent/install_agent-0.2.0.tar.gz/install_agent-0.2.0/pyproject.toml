[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "install-agent"
version = "0.2.0"
description = "Install Claude Code sub-agents from GitHub to your project"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "agent-resources>=0.1.0",
]

[project.scripts]
install-agent = "agent_resources.cli.agent:app"

[tool.hatch.build.targets.wheel]
packages = ["src/install_agent"]
