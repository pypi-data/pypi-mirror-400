[project]
name = "renameit-cli"
version = "0.1.0"
description = "Simple CLI tool to rename files and folders"
authors = [{ name = "M.Ammar Ali" }]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
renameit = "renameit.cli:main"
