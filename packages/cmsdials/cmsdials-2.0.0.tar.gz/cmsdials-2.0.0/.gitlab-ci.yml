include:
  - project: "cms-ppd/technical-support/ci-templates"
    ref: release/v1
    file:
      - "templates/python/uv_build.yml"
      - "templates/python/uv_publish.yml"
      - "templates/gitlab/create_release.yml"

stages:
  - test
  - build
  - publish
  - create_release

###########
# Run tests
###########

test_matrix:
  stage: test
  parallel:
    matrix:
      - PYTHON_VERSION: ["3.9", "3.10", "3.11", "3.12"]
  image: registry.cern.ch/docker.io/library/python:$PYTHON_VERSION
  before_script:
    - python -m pip install --upgrade pip
    - pip install uv
    - uv sync --all-groups --extra pandas --extra tqdm
  script:
    - uv run ruff format --check cmsdials tests
    - uv run ruff check cmsdials tests
    - uv run mypy cmsdials tests
    - uv run pytest tests
  coverage: "/TOTAL.+ ([0-9]{1,3}%)/"
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
  variables:
    SECRET_KEY: "$SECRET_KEY"

###############
# Build package
###############

build:
  extends: .uv_build
  variables:
    UV_SYNC_FLAGS: "--all-groups --extra pandas --extra tqdm"
  needs:
    - test_matrix

#################
# Publish to PyPI
#################

publish:
  extends: .uv_publish
  rules:
    - if: $CI_COMMIT_TAG
  needs:
    - build

##########################
# Create release in GitLab
##########################

create_release:
  extends: .create_gitlab_release
  rules:
    - if: $CI_COMMIT_TAG
  needs:
    - publish
