"""
Bundled datasets for the PyGeomodeling toolkit.

The default SPE9 reservoir model (`SPE9.GRDECL`) is packaged here so
`load_spe9_data()` can access it even when the project is installed from a wheel.
"""
