[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "whytrace"
version = "0.1.2"
description = "A conditional logic visualizer for Python"
authors = [{ name = "Hemapriya N" }]
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "rich>=13.0"
]

[project.urls]
Homepage = "https://github.com/Nhemapriya/whytrace"
Repository = "https://github.com/Nhemapriya/whytrace"
