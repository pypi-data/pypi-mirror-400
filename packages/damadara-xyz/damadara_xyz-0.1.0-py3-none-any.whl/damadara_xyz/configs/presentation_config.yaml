# configs/presentation_config.yaml
column_style: "snake_case"  # or "camelCase", "title"

label_casing: "title"  # For categoricals: "title", "lower", "upper"

date_format: "%Y-%m-%d"  # ISO standard

numeric_precision:
  spend_amount: 2  # Decimals

text_beautify:
  name: true  # Title case names
  city: true  # Title case cities

metadata:
  include_descriptions: true
  export_format: "yaml"  # or "json"