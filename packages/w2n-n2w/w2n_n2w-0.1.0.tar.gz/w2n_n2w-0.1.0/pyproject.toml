[project]
name = "w2n-n2w"
version = "0.1.0"
description = "Convert numbers to words and vice versa"
readme = "README.md"
authors = [
    { name = "VipulSingh2", email = "v.ipulsingh592005@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    # Add runtime dependencies here if needed, e.g., "numpy", "pandas"
]

# This defines the CLI entry point for your package
[project.scripts]
w2n-n2w = "w2n_n2w:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
