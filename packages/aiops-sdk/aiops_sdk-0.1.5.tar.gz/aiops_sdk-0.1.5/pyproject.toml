[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aiops-sdk"
version = "0.1.5"
description = "Lightweight SDK for AIOps monitoring platform"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="Your Name / Org", email="you@company.com" }
]
dependencies = [
  "requests"
]

[tool.setuptools.packages.find]
where = ["."]
