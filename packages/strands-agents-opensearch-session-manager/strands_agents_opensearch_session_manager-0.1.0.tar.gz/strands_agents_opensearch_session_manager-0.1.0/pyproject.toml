[project]
name = "strands-agents-opensearch-session-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"