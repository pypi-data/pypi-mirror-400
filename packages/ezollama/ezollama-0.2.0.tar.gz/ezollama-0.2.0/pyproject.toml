[project]
name = "ezollama"
version = "0.2.0"
description = "A lightweight Python wrapper for Ollama AI models."
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "SuperKaa", email = "you@example.com"}
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "pyttsx3>=2.90"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
