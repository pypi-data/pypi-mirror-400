[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "valos"
version = "0.0.1"
description = "Coming soon."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
