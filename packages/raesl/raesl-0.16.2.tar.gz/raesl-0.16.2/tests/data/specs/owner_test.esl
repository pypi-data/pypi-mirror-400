define component Y
    parameter
        w is a real property

define component XY
    parameter
        v is a real property

    components
        child is a Y with arguments
            * v

world
    variables
        v is a real
        w is a real

    components
        one-owner is a Y with arguments
            * v

        two-owners is a XY with arguments
            * w
