define relation
    myfunc
        requiring parameters
            * p is a real
            * q is a real

    otherfunc
        requiring parameters
            * p is a real
            * one or more q is a real
            * r is a real

world
    variable
        a is a real

    relations
        mf: myfunc
            requiring arguments
                * a

        of: otherfunc
            requiring arguments
                * a
                * a
