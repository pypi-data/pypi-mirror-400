[build-system]
requires = ["setuptools>=58.0", "wheel"]  # 降级setuptools版本要求，提升兼容性
build-backend = "setuptools.build_meta"

[project]
name = "mxshare"
version = "0.0.6"
authors = [{ name = "mxxu", email = "17611379220@163.com" }]
description = "python share"
license = { text = "MIT" }
requires-python = ">=3.6"
dependencies = ["requests>=2.25.0"]

