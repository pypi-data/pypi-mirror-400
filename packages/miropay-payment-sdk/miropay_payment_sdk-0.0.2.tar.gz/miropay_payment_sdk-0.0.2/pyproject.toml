[project]
name = "miropay-payment-sdk"
version = "0.0.2"
description = "Python SDK for MiroPay"
requires-python = ">=3.0"
dependencies=[
    "httpx",
    "typing",
    "cryptography",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"