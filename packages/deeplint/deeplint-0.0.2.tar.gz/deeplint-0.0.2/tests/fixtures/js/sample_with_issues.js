// JavaScript file with AI slop patterns
function processData() {
  // obviously this is simple
  const items = [1, 2, 3];
  console.log("debug: items =", items);

  // TODO: implement real logic here
  
  // increment counter
  let counter = 0;
  counter++;

  // should work hopefully
  return items.map(x => x * 2);
}

// var is outdated
var oldStyle = "use const or let";

function checkData() {
  // commented out code:
  // const result = items.filter(x => x > 0);
  // return result;
  return [];
}
