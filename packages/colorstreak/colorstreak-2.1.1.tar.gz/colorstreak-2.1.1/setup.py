from setuptools import setup, find_packages

setup(
    name='colorstreak',
    version='2.1.0',
    description='Terminal color formatting for Python, with logging capabilities',
    author='Fernando Leon Franco',
    packages=find_packages(),
    install_requires=[],
)
