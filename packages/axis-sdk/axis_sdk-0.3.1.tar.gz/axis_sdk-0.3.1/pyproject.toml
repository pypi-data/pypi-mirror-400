[project]
name = "axis-sdk"
version = "0.3.1"
description = "AXIS SDK - Zero-dependency protocol and type definitions for AXIS agents"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Emily Veiga", email = "emily@lumpi.ai"}
]
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/emilyveigaai/axis-sdk"
Documentation = "https://github.com/emilyveigaai/axis-sdk#readme"
Repository = "https://github.com/emilyveigaai/axis-sdk"
Issues = "https://github.com/emilyveigaai/axis-sdk/issues"

[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = "-v --tb=short --strict-markers"

[tool.coverage.run]
source = ["axis_sdk"]
omit = ["*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
