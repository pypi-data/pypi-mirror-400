#!/usr/bin/env bash
# Rebuild Sphinx documentation from scratch with strict warnings
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
rm -rf docs/build
uv run sphinx-build -W docs/source docs/build
