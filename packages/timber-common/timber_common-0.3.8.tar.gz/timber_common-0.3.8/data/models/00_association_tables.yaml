# 00_association_tables.yaml
# DEBUGGED VERSION - Explicit syntax that factory can parse
# This file MUST load before narrative_models.yaml and notification_models.yaml

version: "1.0.0"
description: "Association tables for many-to-many relationships"

# NO MODELS - Just association tables
association_tables:
  - name: goal_tags
    columns:
      - name: goal_id
        type: Integer
        foreign_key: user_goal.id
        primary_key: true
        nullable: false
        
      - name: tag_id
        type: Integer
        foreign_key: tags.id
        primary_key: true
        nullable: false
        
  - name: notification_tags
    columns:
      - name: notification_id
        type: String
        length: 36
        foreign_key: notifications.id
        primary_key: true
        nullable: false
        
      - name: tag_id
        type: Integer
        foreign_key: tags.id
        primary_key: true
        nullable: false