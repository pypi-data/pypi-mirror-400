version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: examples/ringg-chatbot/Dockerfile
    restart: always
    ports:
      - "8765:8765"
    volumes:
      - ./examples/ringg-chatbot:/app
    command: python server.py