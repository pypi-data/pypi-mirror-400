# Example .env file for MCP Server
# Copy this file to .env and fill in your actual values

# === SMTP Configuration ===
# Choose one provider and uncomment the relevant section

# Gmail Configuration (recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Outlook Configuration
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Yahoo Configuration
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@yahoo.com
# SMTP_PASS=your-app-password

# iCloud Configuration
# SMTP_HOST=smtp.mail.me.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@icloud.com
# SMTP_PASS=your-app-password

# AhaSend Configuration (Commercial email service)
# SMTP_HOST=send.ahasend.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ahasend-username
# SMTP_PASS=your-ahasend-password

# Custom SMTP Configuration
# SMTP_HOST=your-smtp-server.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-username
# SMTP_PASS=your-password

# === Other Services ===
# Serper API for web search (documentation tools)
SERPER_API_KEY=your-serper-api-key

# === Notes ===
# For Gmail:
#   1. Enable 2-Factor Authentication
#   2. Go to Google Account settings > Security > 2-Step Verification > App passwords
#   3. Generate an app password for "Mail"
#   4. Use the 16-character app password (not your regular password)
#
# For Yahoo:
#   1. Enable 2-Factor Authentication
#   2. Go to Account Security > Generate app password
#   3. Use the generated app password
#
# For Outlook:
#   - You can use your regular password or generate an app password
#
# For AhaSend:
#   - Use your AhaSend account credentials
#   - This is a commercial email service provider
#
# SMTP_SECURE settings:
#   - Set to true only for port 465 (SSL)
#   - Set to false for port 587 (TLS/STARTTLS) 