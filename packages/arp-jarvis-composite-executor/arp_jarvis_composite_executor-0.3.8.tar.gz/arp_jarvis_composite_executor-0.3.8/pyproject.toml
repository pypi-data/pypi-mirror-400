[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arp-jarvis-composite-executor"
version = "0.3.8"
description = "JARVIS Composite Executor implementation for the ARP Standard (spec/v1)."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Agent Runtime Protocol" }]
dependencies = [
  "arp-auth==0.2.1",
  "arp-llm==0.1.1",
  "arp-standard-client==0.3.7",
  "arp-standard-server==0.3.7",
  "arp-standard-model==0.3.7",
  "jsonschema>=4.21.1",
  "uvicorn>=0.29.0",
]

[project.optional-dependencies]
dev = ["pyright>=1.1.0", "pytest>=7", "pytest-cov>=4"]

[project.scripts]
arp-jarvis-composite-executor = "jarvis_composite_executor.__main__:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
