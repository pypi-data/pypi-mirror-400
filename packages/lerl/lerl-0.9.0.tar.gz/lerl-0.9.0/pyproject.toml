[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lerl"
version = "0.9.0"
description = "Multi code-snippet generator for notebooks"
readme = "README.md"
authors = [{name="Hammad Ihsan", email="hammadihsan558@gmail.com"}]
dependencies = ["numpy", "ipython"]
