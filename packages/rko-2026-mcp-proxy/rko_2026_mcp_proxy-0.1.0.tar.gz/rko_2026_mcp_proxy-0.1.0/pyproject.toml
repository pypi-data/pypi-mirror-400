[project]
name = "rko-2026-mcp-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
]

[project.scripts]
rko-2026-mcp-proxy = "rko_2026_mcp_proxy.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
