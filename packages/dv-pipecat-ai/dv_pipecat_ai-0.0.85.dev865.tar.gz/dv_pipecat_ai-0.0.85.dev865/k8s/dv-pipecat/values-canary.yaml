releaseTrack: canary
replicaCount: 2

env:
  - name: LOGGER_NAME
    value: "pipecat-canary"

service:
  create: false # So that canary will not create new service

# Disable KEDA for canary - we only want production to be scaled
keda:
  enabled: false