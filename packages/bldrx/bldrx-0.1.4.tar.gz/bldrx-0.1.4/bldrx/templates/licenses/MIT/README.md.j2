# MIT License

This template renders the full MIT license. It expects the following variables:

- `year` — Year for copyright statement (defaults available in `ci_metadata.json`)
- `author_name` — Name of the copyright holder

Example usage when generating a project:

```bash
bldrx new myproj --meta author_name="Jane Doe" --meta year=2026
```
