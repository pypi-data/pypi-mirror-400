[project]
name = "procbench"
version = "0.1.1"
description = "Process Benchmarking Tool"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "psutil>=7.2.1",
]

[project.scripts]
procbench = "procbench.cli:main"

[tool.setuptools.package-data]
procbench = ["templates/**/*"]
