define type
    B is a bundle of
      *  r is a real
      *  s is a string

define verb
    provide to

world
    variables
        a is a real
        b is a B

    components
       foo is a blob
       faa is a blob

    goal-requirement
       provide-real: foo must provide b.r to faa

    need
        c1: b.r must be a real real
        c2: b.s could have a real
        # c3: b must be a real real


define component blob
    empty