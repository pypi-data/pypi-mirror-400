[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qtma"
version = "1.3.0"
dynamic = ["readme"]
description = "ADC Behavioral Event-Driven Simulator with Bukkit-style API"
license = {text = "MIT"}
requires-python = ">=3.8"
authors = [
    {name = "KonataLin", email = "2424441676@qq.com"}
]
keywords = ["adc", "analog-to-digital", "simulation", "sar", "pipeline", "event-driven"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy>=1.20.0",
    "matplotlib>=3.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=22.0.0",
    "flake8>=5.0.0",
    "mypy>=0.990",
]
docs = [
    "sphinx>=5.0.0",
    "sphinx-rtd-theme>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/KonataLin/QuantiaMagica"
Documentation = "https://github.com/KonataLin/QuantiaMagica/docs"
Repository = "https://github.com/KonataLin/QuantiaMagica"
Issues = "https://github.com/KonataLin/QuantiaMagica/issues"

[tool.setuptools]
packages = ["quantiamagica", "quantiamagica.core", "quantiamagica.adc", "quantiamagica.analysis", "quantiamagica.optim", "quantiamagica.ide", "quantiamagica.plotting", "quantiamagica.signals", "quantiamagica.utils"]
include-package-data = true

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[tool.setuptools.package-data]
quantiamagica = ["ide/static/*", "ide/templates/*"]

[tool.black]
line-length = 88
target-version = ['py38', 'py39', 'py310', 'py311']
include = '\.pyi?$'

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
