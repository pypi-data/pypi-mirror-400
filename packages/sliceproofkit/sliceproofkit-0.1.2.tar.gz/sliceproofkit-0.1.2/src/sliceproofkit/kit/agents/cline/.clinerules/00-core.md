# Cline rules (short + hard)

Start from `AGENT.md` (search order + DoD).

Must:
- Slice protocol (oracle -> small change -> verify_fast -> STM update)
- Fail-fast: never swallow errors
- Evidence-driven output: run_id/log/verify + files_changed
