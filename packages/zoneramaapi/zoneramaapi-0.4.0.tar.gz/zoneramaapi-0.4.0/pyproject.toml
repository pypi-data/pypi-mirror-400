[project]
name = "zoneramaapi"
version = "0.4.0"
description = "A Python client for the Zonerama API."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "zeep[async]>=4.3.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
