import time


def 打印数字序列(
        数值: int,
        前缀文本: str = "",
        后缀文本: str = "",
        倒序打印: bool = True,
        打印间隔: float = 0.0,
):
    """
    按顺序或倒序打印数字序列，可自定义前缀、后缀和打印间隔。

    功能说明：
        - 根据用户输入打印一个整数序列；
        - 倒序打印=True（默认）时，从 数值 → 1；
        - 倒序打印=False 时，从 1 → 数值；
        - 可添加前缀与后缀；
        - 打印间隔可控制节奏（默认 0 秒，无间隔）；
        - 全程异常安全，不抛出错误。

    Args:
        数值 (int): 序列最大值，必须为正整数。
        前缀文本 (str): 每个数字前的文本，默认空。
        后缀文本 (str): 每个数字后的文本，默认空。
        倒序打印 (bool): 是否倒序打印（默认 True）。
        打印间隔 (float): 每次打印后的等待时间（秒），默认 0。

    Returns:
        None
    """
    try:
        最大值 = int(数值)
        if 最大值 <= 0:
            return

        # 选择打印方向
        if 倒序打印:
            序列 = range(最大值, 0, -1)
        else:
            序列 = range(1, 最大值 + 1)

        # 打印循环
        for n in 序列:
            print(f"{前缀文本}{n}{后缀文本}")
            if 打印间隔 > 0:
                time.sleep(打印间隔)

    except Exception:
        pass