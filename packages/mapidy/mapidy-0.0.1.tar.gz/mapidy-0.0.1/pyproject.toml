[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mapidy"
version = "0.0.1"
description = "Open-source toolkit to deploy projects from an architectural starting point."
readme = "README.md"
requires-python = ">=3.8"

[project.urls]
Homepage = "https://mapidy.com"
