
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "PYSetupper"
version = "0.0.1"
description = "Ready Setupper"
dependencies = ["PyQt6"]
