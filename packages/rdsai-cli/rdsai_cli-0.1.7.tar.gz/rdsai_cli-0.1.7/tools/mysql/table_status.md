Get table status information using SHOW TABLE STATUS.

**Parameters:**
- **db_name** (optional): Database name to query (uses current database if not specified)
- **table_name** (optional): Specific table name (shows all tables if not specified)

Shows table statistics including storage engine, row counts, data/index sizes, creation time, and other metadata.
