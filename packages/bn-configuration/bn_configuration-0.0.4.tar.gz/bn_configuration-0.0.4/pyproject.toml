[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "<configuratiion>"  # Will be dynamically replaced by CI/CD pipeline
version = "<1.0.0>"    # Will be dynamically replaced by CI/CD pipeline
description = "<configuration loader>"
readme = "README.md"
requires-python = ">=3.7"
authors = [
    {name = "<Jo Vinckier>", email = "<jo.vinckier@byneuron.com>"},
]

[project.urls]
"Homepage" = "<https://gitlab.com/bynubian/bynode/python_packages/configuration>"  # Will be replaced with actual project URL