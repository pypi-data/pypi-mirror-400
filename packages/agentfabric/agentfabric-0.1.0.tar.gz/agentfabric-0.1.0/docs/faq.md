# FAQ

## Q: 运行期能不能新增列/迁移？

MVP 不支持。当前语义是：改 YAML → 你自行迁移或重建库。

## Q: `extra` 里能做数值范围过滤吗？

MVP 不承诺。`extra.key` 会按文本处理，只支持 `eq/ne/in_/nin/is_null/like`。

## Q: 复合主键顺序有意义吗？

语义上主键是“组合唯一”，不依赖顺序；但底层主键索引是 B-Tree，列顺序会影响常见过滤能否走索引左前缀。
