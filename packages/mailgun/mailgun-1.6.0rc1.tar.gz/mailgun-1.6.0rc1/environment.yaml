---
name: mailgun
channels:
  - defaults
dependencies:
  - python >=3.10
  # build & host deps
  - pip
  # runtime deps
  - requests >=2.32.5
  - httpx >=0.24.0
  - typing_extensions >=4.7.1  # [py<311]
  # tests
  - pytest >=7.0.0
  # other
  - pre-commit
  - toml
