{
  "$id": "/components/lines-3d-indices/1.0.1/lines-3d-indices.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "This component describes line endpoints for 3D lines. It has two columns (start,end), which are indices into vertices_3D for the line endpoint. Unlike 2D, curved segments in 3D would use a 2D segment with a transform, or be represented via a BREP",
  "type": "object",
  "properties": {
    "indices": {
      "$ref": "/elements/index-array-2/1.0.1/index-array-2.schema.json",
      "description": "0-based indices into the vertices. Columns: start, end."
    }
  },
  "required": [
    "indices"
  ]
}
