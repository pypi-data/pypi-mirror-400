[project]
name = "europa"
version = "0.1.9"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.12",
    "uvicorn>=0.34.0",
    "websocket-client>=1.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "black>=25.1.0",
    "hazelcast-python-client>=5.5.0",
    "httpx>=0.28.1",
    "psycopg2-binary>=2.9.10",
    "pylint>=3.3.6",
    "pytest>=8.3.5",
]

[project.scripts]
europa-launcher = "europa.framework.launcher:main"  # Maps to `main()` in launcher.py
