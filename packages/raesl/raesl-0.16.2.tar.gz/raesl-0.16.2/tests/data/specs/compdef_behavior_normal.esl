define component X
    parameters
        x is a real
        y is a real

    behavior-requirement
        output-x:
            case A:
                when
                    * c1: y is smaller than 5
                then
                    * r1: x must be at most 5
            case B:
                when
                    * c2: y is greater than 10
                then
                    * r2: x must be at most 10
            case C:
                when no other case applies
                then
                    * r3: x must be equal to 0

world
    empty
