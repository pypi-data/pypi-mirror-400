"""
Demo file to test color-coded vibes and compact summary.
"""

def bad_function():
    """
    This function demonstrates various bad coding patterns.
    It has overly verbose comments and poor naming.
    """
    # This is a very obvious comment that adds no value
    x = 1
    # Another useless comment
    y = 2
    # Calculate the sum - this comment is redundant
    result = x + y
    return result

# AI-generated looking code with generic variable names
def process_items(items_list):
    """
    This function processes a list of items.
    It performs some operations on each item in the list.
    The function returns the processed items.
    """
    # Initialize an empty list to store processed items
    processed_items = []

    # Iterate through each item in the input list
    for item in items_list:
        # Check if the item meets certain criteria
        if item > 0:
            # If the item is positive, double its value
            processed_item = item * 2
            # Append the processed item to the result list
            processed_items.append(processed_item)

    # Return the list of processed items
    return processed_items
