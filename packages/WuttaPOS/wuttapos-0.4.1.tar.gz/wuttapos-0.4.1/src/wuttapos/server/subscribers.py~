# -*- coding: utf-8; -*-
"""
Pyramid event subscribers
"""

import server


def add_server_to_context(event):
    renderer_globals = event
    renderer_globals['server'] = server


def includeme(config):
    config.include('wuttaweb.subscribers')
    config.add_subscriber(add_server_to_context, 'pyramid.events.BeforeRender')
