name: "Semantic Pull Request and automatic labeling"

on:
  pull_request:
    types: [opened, edited, ready_for_review]

jobs:
  main:
    uses: BareSquare/.github/.github/workflows/pr-title-automations.yml@v1
