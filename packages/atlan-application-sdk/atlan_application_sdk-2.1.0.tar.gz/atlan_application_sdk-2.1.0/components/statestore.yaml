apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  version: v1
  type: state.sqlite
  ignoreErrors: true
  metadata:
    # Connection string
    - name: connectionString
      value: "./local/dapr/statestore.db"
