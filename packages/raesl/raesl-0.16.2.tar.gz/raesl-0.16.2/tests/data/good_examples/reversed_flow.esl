define verb
  retrieve from
  convert into

world
  variable
    x is a real

  components
    foo is a blub with argument
      x
    faa is a blab with argument
      x

  goal-requirement
    retrieve-x: foo must retrieve x from faa


define component blab
  parameter
    x is a real


define component blub
  parameter
    x is a real

  variable
    y is a real

  components
    fuu is a blob with arguments
      x, y
    fee is a blab with argument
      y

  goal-requirement
    provide-y: fuu must provide y to fee


define component blob
  parameters
    q, r is a real

  transformation-requirement
    convert-x: must convert q into r