apiVersion: v1
kind: Service
metadata:
  name: {{ include "mind.fullname" . }}-api
  labels:
    {{- include "mind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.api.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "mind.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
