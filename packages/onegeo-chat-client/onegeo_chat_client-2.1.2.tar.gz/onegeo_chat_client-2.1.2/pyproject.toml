[build-system]
requires = ["setuptools", "wheel", "twine"]
build-backend = "setuptools.build_meta"

[project]
name = "onegeo-chat-client"
description = "RAG based chatbot client"
version = "2.1.2"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Mathilde POMMIER", email = "mpommier@neogeo.fr"},
    {name = "Sébastien DA ROCHA", email = "sdarocha@neogeo.fr"},
]

requires-python = ">=3.10"
dependencies = [
  "requests",
]

[tool.setuptools.packages.find]
where = ["."]
