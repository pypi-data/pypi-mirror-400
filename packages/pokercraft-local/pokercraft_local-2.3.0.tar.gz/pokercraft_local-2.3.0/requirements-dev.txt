# Dependencies for development and distribution.
# This is also used in CI, so the versions are strictly specified.
pre-commit == 4.0.*
maturin == 1.9.*
pyinstaller == 6.16.*
twine == 5.1.*
build == 1.2.*
