[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vyre"
version = "0.2.0"
description = "Build applications in Python with Vyre Framework."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
authors = [
  { name = "Viren Sahti" }
]

dependencies = []

[project.scripts]
vyre = "vyre.vyre:main"