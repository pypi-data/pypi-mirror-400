{% if results is defined %}
  <div id="home" class="page padding">
{% else %}
  <div id="home" class="page padding active">
{% endif %}
    <h5>Analyze variant(s)</h5>
    <p>
    Welcome to the Genetic Therapy Generator Toolkit, please specify one or more variants in a single <a href="https://hgvs-nomenclature.org" class="link" target="_blank">HGVS description</a> below:
    </p>
    <div class="field border round small" style="margin-bottom:1em;">
      <input id="variant_input" type="text" {{'value=' + '"' + variant + '"' if variant else ''}} autofocus>
    </div>
    <label class="checkbox">
      <input type="checkbox" id="protein_domains">
      <span>Include protein domains</span>
    </label>
    <div>
      <progress id="submitSpinner" style="visibility:hidden"></progress>
      <button id="submitBtn" class="center" type="button" onclick="redirect()" >Submit</button>
    </div>

    {% if error is defined %}
    <div id="error" class="active">
      <article>
        <details open>
          <summary>
            <p>
              <i>error</i>
              {{ error["summary"]}}
            </p>
          </summary>
          <code>{{ error["details"] }}</code>
        </details>
      </article>
    </div>
    {% endif %}

    <h5>Examples</h5>
    <p>The input must be an allele in HGVS format, describing variants on an Ensembl transcript.
    <table>
      <thead>
        <tr>
          <th>Variant</th>
          <th>Explanation</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><a class="link" onclick="fillSearchField('ENST00000375549.8:c.100del')">ENST00000375549.8:c.100del</a></td>
          <td>Frameshift deletion in exon 2 of SHDH</td>
        </tr>
        <tr>
          <td><a class="link" onclick="fillSearchField('NM_004006.3:c.2500del')">NM_004006.3:c.2500del</a></td>
          <td>Frameshift deletion in exon 20 of DMD</td>
        </tr>
      </tbody>
    </table>
  </div>

