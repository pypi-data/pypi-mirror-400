# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.authenticate_api_key_response import AuthenticateApiKeyResponse
from ..types.liveblocks_access_response import LiveblocksAccessResponse
from .raw_client import AsyncRawAuthClient, RawAuthClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AuthClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAuthClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAuthClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAuthClient
        """
        return self._raw_client

    def get_token(
        self, *, api_key: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AuthenticateApiKeyResponse:
        """
        Authenticate an API key and return an access token

        Returns a client JWT for the provided API key, where the JWT contains claims for permissions and access and can be
        refreshed as part of an OAuth flow.

        Parameters
        ----------
        api_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuthenticateApiKeyResponse
            Successful Response

        Examples
        --------
        from termina import Termina

        client = Termina(
            token="YOUR_TOKEN",
        )
        client.auth.get_token(
            api_key="api_key",
        )
        """
        _response = self._raw_client.get_token(api_key=api_key, request_options=request_options)
        return _response.data

    def get_liveblocks_token(
        self, *, room_ids: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> LiveblocksAccessResponse:
        """
        Get a Liveblocks access token for one or more rooms.

        This endpoint accepts a list of room_ids and returns a Liveblocks access token for the authenticated user.
        Each room_id must follow the pattern: {environment}:v1:group:{group_id}:deal:{deal_id}

        If an empty list is provided, issues a general Liveblocks user token with no room-specific permissions.
        If any room is inaccessible, the entire request fails (all-or-nothing).

        Returns:
            LiveblocksAccessResponse: Contains the Liveblocks access token

        Raises:
            HTTPException: 422 if any room_id pattern is invalid
            HTTPException: 404 if any room is not accessible to the user

        Parameters
        ----------
        room_ids : typing.Sequence[str]
            List of Liveblocks room IDs to authorize access to. Empty list issues a general user token. All rooms must be accessible or the request will fail.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LiveblocksAccessResponse
            Successful Response

        Examples
        --------
        from termina import Termina

        client = Termina(
            token="YOUR_TOKEN",
        )
        client.auth.get_liveblocks_token(
            room_ids=["termina-dev:v1:group:1:deal:2", "termina-dev:v1:group:1:deal:3"],
        )
        """
        _response = self._raw_client.get_liveblocks_token(room_ids=room_ids, request_options=request_options)
        return _response.data


class AsyncAuthClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAuthClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAuthClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAuthClient
        """
        return self._raw_client

    async def get_token(
        self, *, api_key: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AuthenticateApiKeyResponse:
        """
        Authenticate an API key and return an access token

        Returns a client JWT for the provided API key, where the JWT contains claims for permissions and access and can be
        refreshed as part of an OAuth flow.

        Parameters
        ----------
        api_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AuthenticateApiKeyResponse
            Successful Response

        Examples
        --------
        import asyncio

        from termina import AsyncTermina

        client = AsyncTermina(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.auth.get_token(
                api_key="api_key",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_token(api_key=api_key, request_options=request_options)
        return _response.data

    async def get_liveblocks_token(
        self, *, room_ids: typing.Sequence[str], request_options: typing.Optional[RequestOptions] = None
    ) -> LiveblocksAccessResponse:
        """
        Get a Liveblocks access token for one or more rooms.

        This endpoint accepts a list of room_ids and returns a Liveblocks access token for the authenticated user.
        Each room_id must follow the pattern: {environment}:v1:group:{group_id}:deal:{deal_id}

        If an empty list is provided, issues a general Liveblocks user token with no room-specific permissions.
        If any room is inaccessible, the entire request fails (all-or-nothing).

        Returns:
            LiveblocksAccessResponse: Contains the Liveblocks access token

        Raises:
            HTTPException: 422 if any room_id pattern is invalid
            HTTPException: 404 if any room is not accessible to the user

        Parameters
        ----------
        room_ids : typing.Sequence[str]
            List of Liveblocks room IDs to authorize access to. Empty list issues a general user token. All rooms must be accessible or the request will fail.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LiveblocksAccessResponse
            Successful Response

        Examples
        --------
        import asyncio

        from termina import AsyncTermina

        client = AsyncTermina(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.auth.get_liveblocks_token(
                room_ids=[
                    "termina-dev:v1:group:1:deal:2",
                    "termina-dev:v1:group:1:deal:3",
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_liveblocks_token(room_ids=room_ids, request_options=request_options)
        return _response.data
