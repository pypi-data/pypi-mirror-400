# pyproject.toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "sandlertools"
version = "0.5.1"
authors = [
  { name="Cameron F Abrams", email="cfa22@drexel.edu" },
]
description = "A metapackage combining several computational tools based on Chemical, Biochemical, and Engineering Thermodynamics (5th edition) by Stan Sandler"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "numpy",
    "scipy",
    "sandlercubics>=0.2.0",
    "sandlercorrespondingstates>=0.1.0",
    "sandlersteam>=0.5.0",
    "sandlerprops>=0.2.0",
    "sandlermisc>=0.2.1",
    "sandlerchemeq>=0.1.0"
]

[tool.pytest.ini_options]
log_cli = true

[tool.hatch.build.targets.wheel]
packages = ["src/sandlertools"]

[project.scripts]
sandlertools="sandlertools.cli:cli"

[tool.hatch.build]
include = [
  "src/sandlertools/data/**",
]

[tool.hatch.build.targets.sdist]
include = [
  "src/sandlertools/**",
]