[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "netic"
version = "0.1.0"
description = "CLI Netic"
dependencies = ["requests"]

[project.scripts]
netic = "netic.cli:main"
