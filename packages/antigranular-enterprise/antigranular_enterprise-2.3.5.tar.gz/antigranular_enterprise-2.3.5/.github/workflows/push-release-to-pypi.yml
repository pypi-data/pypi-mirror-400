name: Push Release to PyPI

on:
  push:
    tags:
      - '**' # Matches any tag pushed

jobs:
  call-cosign-sigcheck:
    uses: ObliviousAI/workflow_templates/.github/workflows/push_release_to_pypi.yml@dev
    secrets: inherit