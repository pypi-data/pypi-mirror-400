[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shelcolors.ansi"
version = "0.1.6"
description = "ANSI color, save&load, and clear screen helper"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "Sheldon"}]

[tool.setuptools.packages.find]
where = ["."]