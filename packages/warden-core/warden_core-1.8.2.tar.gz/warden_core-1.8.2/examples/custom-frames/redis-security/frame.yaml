# Warden Custom Frame Metadata
# Generated by: warden frame create redis-security

name: "redis-security"
id: "redis-security"
version: "1.0.0"
author: "alper"
description: "Custom validation frame for redis-security"

# Frame classification
category: "global"  # global | language-specific | framework-specific
priority: "critical"     # critical | high | medium | low
scope: "file_level"     # file_level | repository_level
is_blocker: true

# Optional: Applicability filters
applicability:
  - language: "python"
  # - language: "typescript"
  # - framework: "fastapi"

# Optional: Required Warden version
min_warden_version: "1.0.0"
# max_warden_version: "2.0.0"

# Optional: Configuration schema
config_schema:
  enabled:
    type: "boolean"
    default: true
    description: "Enable this frame"

  # Add your custom config fields here
  # check_ssl:
  #   type: "boolean"
  #   default: true
  #   description: "Check SSL requirement"

# Optional: Tags for discoverability
tags:
  - "custom"
  - "security"
  # Add relevant tags
