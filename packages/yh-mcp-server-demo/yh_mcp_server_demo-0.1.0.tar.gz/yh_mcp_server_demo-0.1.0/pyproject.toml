[project]
name = "yh-mcp-server-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "zhouyanghong", email = "zhouyanghong@qfang.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.25.0",
]

[project.scripts]
yh-mcp-server-demo = "yh_mcp_server_demo:main"

[build-system]
requires = ["uv_build>=0.9.20,<0.10.0"]
build-backend = "uv_build"
