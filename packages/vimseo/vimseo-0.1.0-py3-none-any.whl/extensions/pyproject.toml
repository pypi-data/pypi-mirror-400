[project]
dynamic = ["version"]

[build-system]
requires = [
    "setuptools>=45, <58.0",
    "setuptools_scm[toml]>=6.2",
    "wheel",
    "oldest_supported_numpy",
    "cython",
]
build-backend = "setuptools.build_meta"
