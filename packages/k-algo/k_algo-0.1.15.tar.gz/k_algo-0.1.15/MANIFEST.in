# 包含基礎文件
include README.md
include LICENSE

# 排除不需要進 sdist 的產物
global-exclude *.so *.pyd *.dll *.dylib
global-exclude *.pyc *.pyo __pycache__/

# 若你不想把自動產生的 .c/.cpp 放進 sdist（常見做法）
global-exclude *.c *.cpp

# 但你有手寫 C 檔 (algo/cext/fastsum.c) 需要保留 → 反向包含
include algo/cext/fastsum.c
