from fastapi import FastAPI
from .modules.users.routes import router

app = FastAPI(title="{project_name}")

app.include_router(router)

@app.get("/")
def read_root():
    return {{"message": "Welcome to {project_name}!"}}
