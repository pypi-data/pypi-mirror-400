# @package _global_
defaults:
  - default.yaml

processor:
  _target_: characterization.processors.feature_processor.FeatureProcessor
  config:
    features:
      - separation
      - intersection
      - collision
      - mttcp
      - thw
      - ttc
      - drac

characterizer:
  _target_: characterization.features.interaction_features.InteractionFeatures
