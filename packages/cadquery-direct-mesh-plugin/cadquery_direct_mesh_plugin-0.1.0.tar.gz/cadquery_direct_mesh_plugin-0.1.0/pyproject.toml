[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cadquery_direct_mesh_plugin"
version = "0.1.0"
dependencies = [
  "cadquery",
]
requires-python = ">=3.10"
authors = [
  { name="Jeremy Wright" }
]
maintainers = [
  { name="Jeremy Wright" }
]
description = "CadQuery plugin to go directly from a CadQuery assembly to a mesh without relying on external dependencies."
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["cadquery", "CAD", "engineering", "design", "mesh", "meshing"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python"
]

[project.optional-dependencies]
dev = [
  "pytest",
  "black",
]

[tool.hatch.metadata]
allow-direct-references = true

[project.urls]
Repository = "https://github.com/jmwright/cadquery-direct-mesh-plugin.git"
"Bug Tracker" = "https://github.com/jmwright/cadquery-direct-mesh-plugin/issues"